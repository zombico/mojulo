---
name: Install failed
about: "`npx mojulo init` did not finish, or the agent never saw the tools"
title: "install: "
labels: install
---

**Operating system and version**
<!-- e.g. macOS 15.6 (Apple silicon), Ubuntu 24.04, Windows 11 -->

**Node version** (`node --version`)

**MCP host** (Claude Code / Codex / Claude Desktop / other, with its version)

**The exact command you ran**

```
npx mojulo init
```

**Everything it printed, from the first line to the last**

```
(paste here — including any `npm warn` lines)
```

**If init finished but the agent can't see mojulo:** what does `npx mojulo tools` print?

```
(paste here)
```
