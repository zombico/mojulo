# Mojulo — Status Check & Orientation Report

**Date:** 2026-07-10 · **Branch:** `visualization-layer` (last commit `45a56a7`; the working tree carries a **fresh uncommitted batch**, not yet staged: ~+4,152 / −2,517 across 97 tracked files vs HEAD, plus ~7,612 lines across 61 untracked paths)
**Audience:** a new agent/model getting oriented in this repo.
**Supersedes:** the "Unreleased" sections of [control/CHANGELOG.md](../control/CHANGELOG.md) as the current-state snapshot. The changelog remains the release record; this file is what's true *right now*, including uncommitted work.
**Regeneration contract:** this file is pinned to the working tree above. Committing a staged batch supersedes §6 — rewriting this file (date, branch state, §6) is part of that commit, not an afterthought. (The prior twelve-thread §6 batch was committed as `afc26d3` + `45a56a7`; §6 below now describes the working tree that has accreted since.)

Read order for deeper work: [CLAUDE.md](../CLAUDE.md) → this file → the per-subsystem doc it points at.

---

## 1. What mojulo is

**The agent's workshop** — a **single-user, self-hosted substrate** that turns conversations into things that keep existing after the chat ends. Its real interface is a **localhost HTTP MCP server** (~144 tools); the dashboard, chat builder, and wizard are different faces over the same primitives, and the operator drives everything from a host MCP agent (Claude Code / Codex). The substrate's self-description is "PLAYful Cloud" — **P**ersistent, **L**ocal, **A**gent-**Y**oked: bindings keep running while the operator sleeps, everything lives on the operator's host, and **the agent is the only intelligence in the loop** — inference, vision, and authorship stay on the agent, so mojulo holds no LLM credentials on those paths. Four paradigms plus the recipe artifacts are one category: **durable bindings minted from a conversation**.

Four creatable artifact paradigms:

| Paradigm | What it is | Entry tool |
|---|---|---|
| **Bot** | Deployed chatbot process (Docker/Fly), own SQLite, hash-chained turns | `infer_intent` → `save_modular_bot` |
| **Connected Service** | Skill synthesized into the host adapter, or a materialized mcp-orbit composition over the operator's installed MCPs | `get_catalyst` / `meta_context_declare_inventory` → `bind_primitives` |
| **App** | Local long-running process + MCP sidecar; inference parked back on the agent via the agent-tasks queue | `install_scaffold` → `meta_context_commit({type:'app_materialization'})` → `start_app` |
| **Game** | Playable standalone artifact: shell + typed store + levels-as-worlds carrying a `game:` contract; completability-gated at mint (§6 item 12) | `create_game` |

Beside the paradigms, the same discipline mints **creative artifacts** — diagrams, scientific views, walkable worlds, figures, synthesized audio, films, publications — stored as tiny deterministic recipes, never renders (§4).

Two-package monorepo: `control/` (Next.js 16, port 3001 — bot factory, dashboard, MCP server) and `lite-template/` (Express 5, port 3000 — the bot runtime staged into per-bot artifacts and published as the GHCR image).

## 2. The orientation layer (Ring 0) — how a new agent self-orients

[control/lib/mcp/tools/context.js](../control/lib/mcp/tools/context.js) is the whole progressive-disclosure design in one file:

- The MCP `initialize` preamble is deliberately short (~250 words — it now names all four paradigms + the creative-artifact entries). Everything heavier lives behind **`forward_context`**, which is an **index, not a glossary**: a lean opener, a *routing index* (user-framing → entry-tool rows), a drawer directory, the commitment-level floor, and two standing safety one-liners.
- **Drawers** (pull on demand): `get_register_kit` (concept glossary), `get_tool_index` (full ring-grouped one-liner index), `get_deliberation_overview` (Ring 6 structural model), `get_ui_map` (dashboard pages), `get_substrate` (PLAYful Cloud positioning).
- **Register tuning**, two axes resolved per call as *override > operator anchor > defaults*: `vocabulary_register` (plain / mixed / mojulo — user-facing phrasing only; concept names and tool descriptions are invariant) and `procedural_disclosure` (terse / reflective / pedagogical narration).
- **Commitment-level floor** — four gate distinctions that must stay legible at every register: *proposed vs materialized*, *dry-run vs promoted*, *watched vs read-once*, *sealed in the audit trail vs not recorded*.
- The `custom_protocol` author's guide also lives here; its Step-0 posture check disambiguates **protocol** (fires inside a bot turn) vs **catalyst** (after-the-conversation workflow) vs **skill** (personal automation) — the most common misfire.
- A registry-sweep test in `context.test.js` enforces that every listed tool appears in `TOOL_INDEX`; adding/removing a main-flow tool means updating `TOOL_INDEX` + `ROUTING_INDEX`. A new `create_view` kind or `compose_world` base needs a **view-vocab card**, not an index row.

## 3. The ring model (registration order in [server.js](../control/lib/mcp/server.js))

Pure JSON-RPC 2.0 dispatch (`dispatchMcpRequest`); the Next.js route owns HTTP + bearer auth; `userId` is always `'local'`. Loopback-only, no auth layer — never expose or tunnel it. `listed:false` keeps retired tool names executing as unlisted aliases. All invocation flows through `instrumentedInvoke` (telemetry, new this branch).

- **Rings 0–1, build & operate.** Intent → protocol recommendation → identity/form/appointment/triage/optical-read config generation → `save_modular_bot` compiles a zip. The composer ([control/lib/composer/](../control/lib/composer/)) stacks protocol cartridges (`00_base` … `05_optical-read`) into `instructions.txt`; [deployers/docker.js](../control/lib/deployers/docker.js) stages lite-template, writes config, bakes `config/embeddings.json`, zips. Five protocols ship: `knowledge`, `formGathering`, `appointments`, `triage`, `opticalRead`. Operate reads proxy through [bot-proxy.js](../control/lib/deployers/bot-proxy.js) — **conversation data never enters the control-plane DB**. Fleet tools aggregate; `verify_fleet_chains` walks the per-turn `content_hash`/`chain_hash` tamper-evident chain across bots, extending across federated triage handoffs (`handoff_hash` = sender's tip-of-chain).
- **Ring 6, deliberation.** The contextmap (`meta_context_brief` / `meta_context_commit` — append-only *why*, six typed commit events), inventory declaration (replace-semantic snapshot of the operator's installed MCPs), vendor capability research (`record_mcp_capabilities`, transactional supersession with history), the mcp-orbit composer (five component kinds: mcp × trigger × pattern × idempotency × render; five per-provider states routing remediation), `bind_primitives` (fills a role template with the operator's *actual* installed tool schemas), `bind_trigger` (cron activation, fires only when the scheduler daemon is enabled), `declare_skills` (host-skill mirror), and `semantic_search` (fuzzy recall over all durable state — principles, capabilities, catalysts, sketch/view vocab, painted-landscape glyphs).
- **Ring 7, apps & runtime.** `install_scaffold` → materialization commit → `start_app` (app + MCP sidecar spawned atomically). The app POSTs inference to `/api/app-inference/envelope`; the agent fulfills via `pull_agent_task` → `submit_envelope_inference` (canonical loop: the `run-inference-worker` catalyst, or `MOJULO_AGENT_RUNTIME=claude-code-headless`). Daemon host gating: `MOJULO_DAEMONS=enabled`, per-daemon `MOJULO_TRIGGER_RUNTIME` / `MOJULO_APP_RUNTIME`. Agent-routed builder chat: `emit_chat_signal` / `request_chat_decision`.
- **Ring 8, plan mode.** Plans are the PROPOSED layer (contextmap is sealed reality). Lifecycle: `forge_plan` → `revise_plan` → `compile_plan` (validates the manifest against the *live* tool registry) → `execute_plan` (per-execution `confirm:true` gate, stops on first failure). `sketch_plan` previews the manifest as a pipeline diagram.
- **Ring 9, stash · gather · cook (+ legacy research).** Typed intake (`mint_stash` → `gather`, 8 item types validated at the gate) → `cook` nucleates ONE outcome per singular aim into a self-contained folder at `/outcomes/<cook_ref>/` (14 publication kinds, essay → comic → `site`; the **agent** authors `report_md` — no server-side LLM). `forge_publications` cooks 2–4 candidate kinds at once; `recommend_kind` is rules-based. Legacy diffuse-research tools coexist behind `enter_research_mode`.
- **Ring 10, visualization** — the largest surface; see §4.
- **Ring 11, operations mode — deprecated (this branch).** The domain-scoped deliberation surface (7 MCP tools `enter_operations_mode` / `forge_ops_tag` / `bind_to_ops_tag` / `unbind_from_ops_tag` / `get_ops_tag` / `list_ops_tags` / `revise_ops_tag`, the `/operations` dashboard + REST routes, and its routing rows) was removed as unused — no subsystem consumed the descriptor/thesis semantics. The `ops_tags` / `ops_tag_members` tables + [OpsTagRepository](../control/lib/db/repositories/ops-tags.js) are **retained as the resource-grouping store for motion**: `forge_motion` / `stitch_motion` forge one tag per "Motion Project" and bind the subject stash + rendered motion as members; the `/motion` gallery groups by them via `listTagsForMember`. Nothing registers at Ring 11 anymore.

## 4. Visualization substrate ([control/lib/graph/](../control/lib/graph/))

**Recipe→render architecture**: stored manifests are tiny recipes; per-kind assemblers regenerate geometry deterministically on render. [worlds/world-scene.js](../control/lib/graph/worlds/world-scene.js) is the single `kind → assemble*Scene` dispatch seam feeding both the live World route and `.glb` export; per-kind descriptors (assembler, title, `walk`/`fogBoxes` capability flags) live in the [worlds/world-kinds.js](../control/lib/graph/worlds/world-kinds.js) registry, one row per kind.

Four renderer backends over one engine-agnostic payload (`{faces, cameras, viewBox, unitScale, …}`):

1. **SVG** — two-point-perspective flatten (`projectTwoPoint`); the "Scene / looked-at" tier. `sketch/` handles manifest derive/diff/validate/PNG.
2. **CSS-3D** — [scene/scene-css3d.js](../control/lib/graph/scene/scene-css3d.js): every planar face → a `matrix3d`-positioned div; dependency-free HTML; live turntables via `create_solid_turntable`. Baked lighting model in [scene-css3d-lighting.md](scene-css3d-lighting.md).
3. **three.js / WebGL** — [scene/scene-three.js](../control/lib/graph/scene/scene-three.js): traversable Worlds with orbit/WASD, vendored three, exportable via `export_model` (.glb). The "World / moved-through" tier. The emitter is registry-driven: per-channel page scripts live in [scene/channels.js](../control/lib/graph/scene/channels.js) (`RUNTIME_CHANNELS` — one row per channel: filter, script, emitted header, `__mojStep` slot), the headless-capture bridge names in [scene/capture-contract.js](../control/lib/graph/scene/capture-contract.js), string-safety + delivery-mode helpers in [scene/emit-util.js](../control/lib/graph/scene/emit-util.js). Emission is pinned byte-level by a 36-fixture characterization net (`emit-channels.char.test.js`) + a V8 parse gate (`emit-parse.test.js`); AO bakes are LRU-memoized in-process (geometry-keyed, colors excluded — ~24× on repeat renders).
4. **GLSL raymarch** — painted-landscape raymarch + the volumetric effects overlay (fog) that composites OVER three.js worlds, occluding via a grid-culled scene SDF. Principles in [raymarch-effects-layer.md](raymarch-effects-layer.md).

Shared lighting primitive: `polygonizer/vexar.js` (directional Lambert, one dot product per face). Material response now rides that same baked solve as an opt-in per-surface `material` channel (§6 item 1). Subsystem weight: `polygonizer/` (~321 files — the geometry core), `views/` (~81 — math/science/bio + view-vocab cards), `worlds/` (~32 — physics sim, event bus, game idioms, controllable worlds, `operator-world.js`), `game/` (store kernel, mechanics, kits, and the new glyph/sfx UI-language shelf), `architecture/` (fractal-condo + the new `fractal-school.js`), plus `scene/`, `effects/`, `landscape/`, `machina/`, `figures/`, `vehicles/`, `city/`.

Entry tools by intent: `create_sketch` (diagrams/charts; vocab via `semantic_search({kinds:['sketch_vocab']})` + `get_sketch_vocab`) · `create_view` (45 science/math/bio study kinds; manuals via `get_view_vocab`) · `compose_world` (base × theme: city, transport-hub, controllable, action games, operator-state, planetary, painted-landscape, math, school) · `create_figure` (posed protoform bodies, gait GIFs) · `create_beats` (synthesized audio artifacts — ambient loop / composition / pattern groove / SFX; cards via `get_beats_vocab` + `semantic_search({kinds:['beats_vocab']})`) · `create_game` (playable level artifacts over the game store/contract spine; completability-gated at mint — see §6; `get_game_vocab` now serves five families — slice / mechanic / kit / glyph / sfx) · `create_workbench` / `create_assembler` (lathe polygomers → assemblies) · `create_carved_solid` (extruded metal wordmarks) · `create_manji_tree` (cardinal-grammar IR) · `forge_motion` / `stitch_motion` (camera clips, chart decks, MP4 films) · `reference_protocol` / `capture_reference` (the agent as vision adapter for photo references — no image ever sent to mojulo) · `measure_view` (real-unit time-series read-back) · `verify_machina` (checked feasibility verdicts).

## 5. Bot runtime ([lite-template/](../lite-template/))

Express 5 + better-sqlite3. Fully **local RAG**: `helper/vector-rag.js` boots from the baked `config/embeddings.json` and embeds queries in-process via `helper/embedder-local.js` (`Xenova/multilingual-e5-small` ONNX q8) — same vector space as the control-plane build; fails loudly, no keyword fallback. Turns are hash-chained (`content_hash` + `chain_hash`, SHA-256); `/handoff` writes `event_type='handoff'` rows carrying the sender's tip-of-chain for federated triage. Prometheus `/metrics`. Embeddable widget via `<script src="${botUrl}/widget">`. Landmines: onnxruntime-node is glibc-only (Debian slim, never Alpine); model weights are gitignored and fetched at install.

## 6. Current branch state — `visualization-layer` (this is what supersedes the changelog)

Last commit: `45a56a7` — "verbs, instruments, orientation" — which, with `afc26d3` ("verify, consolidate, inhabit"), committed the entire twelve-thread batch this section used to describe (view-tool drawerization, MCP telemetry + `/observability`, renderer ladder→convergence, emitter char-net hardening, beats B0–B6, game paradigm G0–G4 + mechanics M0-pre–M4, `site` publication kind, star-surface view, SI experiment sweeps, math worlds, orientation ramp R1–R5). That work is now history — the changelog and the per-thread plan files carry it. The working tree has since accreted a fresh set of converging uncommitted threads:

1. **Material response — a per-surface `material` channel for the baked lighting solve (Phases 1–5 LANDED 2026-07-07).** Plan: [material-response.plan.md](../control/lib/graph/material-response.plan.md). Graduates `effects/carved-solid.js`'s closed `MATERIALS` shelf (gold / steel / chrome / stone / wood / rubber / glass / neon / cel …) from one renderer to a polygonizer primitive — the same accretion move as vexar. Nothing neural, nothing runtime: material is a different **response curve** per surface under the same baked light. A face `spec` payload channel + **live World specular** (Blinn-Phong half-vector spliced into `emitThreeWorld` ONLY when a group carries per-vertex `aSpec` — zero bytes otherwise, the glow/shadow one-shot posture, no char-net re-pin) + a **.glb `pbrMetallicRoughness` split** (pbr-tagged faces → a real PBR node; everything else stays `KHR_materials_unlit`, byte-identical) + an operator-facing manifest surface. Opt-in, identity by default. Files: [scene/channels.js](../control/lib/graph/scene/channels.js) (Material/Spec channel scripts), `scene/material-channels.test.js`, [scene/scene-gltf.js](../control/lib/graph/scene/scene-gltf.js), [figures/face-mesh.js](../control/lib/graph/figures/face-mesh.js), `scene-three.js`, `worlds/world-scene.js`.
2. **Game UI language — forms · states · gestures · light · sfx as a player-facing vocabulary.** Plan: [control/lib/graph/game-ui-language.plan.md](../control/lib/graph/game-ui-language.plan.md) (DESIGN + five-spike evidence 2026-07-07; substantial spike code has landed, nothing formally "promoted"). Companion to game-mechanics (VERBS) and game-assets (BODIES): this gives game *state* an **appearance** — a closed, substrate-wide visual vocabulary so what an object *means* (collectible / locked / dangerous / goal / charging / hit) is legible on sight in every mojulo game with no per-level styling. Two guarantees the design buys: **visuals cannot lie** (every effect renders a bus fact — no separate visual state to drift) and **player literacy transfers** (learned once, read everywhere). Landed pieces: [game/glyph-forms.js](../control/lib/graph/game/glyph-forms.js) (8 icon-grade vexar solids — gem / coin / key / heart / star / orb / flag / skull, lowered by `lowerGlyphBodies`), [game/glyph-sfx.js](../control/lib/graph/game/glyph-sfx.js) + `glyph-sfx-sprites.js` (raymarch "juice" verbs — sparkle / heal / ward / kokusen), [game/mechanic-decor.js](../control/lib/graph/game/mechanic-decor.js) (**U5: default-ON UI-language dressing** — a verbs-only `game.mechanics` level auto-gets glyph pickup/exit bodies + fx float/burst + sfx, merged in `world-scene.js` *before* entity lowering; `game.decorate:false` opts out, hand-authored entities/fx/sfx compose on top), `game/glyph-cards/` + `game/sfx-cards/` vocab-card stores + loaders, and new glyph/fx/sfx runtime channels in `scene/channels.js`. Two new embeddings kinds (`game_glyph`, `game_sfx`) via the same CHECK-rebuild migration; `get_game_vocab` now serves **five families** (slice / mechanic / kit / glyph / sfx).
3. **Fractal-school — auto-generative K-12 campuses (Slice 1 LANDED + Slice 2 fit-out 2026-07-07).** Plan: [control/lib/graph/fractal-school.plan.md](../control/lib/graph/fractal-school.plan.md). New [architecture/fractal-school.js](../control/lib/graph/architecture/fractal-school.js) (+ test), a `school-complex` world kind wired into `worlds/world-kinds.js` + `sketch/sketch-manifest.js` (`WORLD_RENDER_KINDS`), a `school` `compose_world` base via [tools/scene-school.js](../control/lib/mcp/tools/scene-school.js) (`mintSchoolComplex`), and a `views/view-vocab/school.md` card. `courtyard`/`spine`/`cluster` campus patterns × `elementary`/`middle`/`high-school` programs; desks-with-chairs, wall-mounted teaching boards, real gyms, a visible stair core per building, doors/portals (glass-transom entrance under a canopy, a doorway carved into every room's corridor wall), front-edge parking-lots + asphalt driveways + registry vehicles (scale-pinned to `VEHICLE_SCALE = 8.5`, filtered against door-clearance aprons), and shared facade cards on every wall segment. Walkability + flood-fill tests guard the ways-in against re-sealing.
4. **Arabesque — spike → `create_sketch` fold-in (LANDED 2026-07-08).** Plan: [control/lib/graph/arabesque-renderer.plan.md](../control/lib/graph/arabesque-renderer.plan.md). The *geometric* strand of Islamic ornament (star-and-polygon girih) from one PIC/Hankin **polygons-in-contact** spec — an exact "recipe not render" fit. Folded in as a compact construction mark `arabesque` that lowers to base `polygon`/`polyline`/`circle` marks at mint time (the `mandalaField` model): [polygonizer/arabesque.js](../control/lib/graph/polygonizer/arabesque.js) (NEW — the geometry promoted from the spike: tilings, PIC, rosettes, circles, strapwork + `expandArabesque`; the spike-lib now re-exports it), `'arabesque'` in `sketch-manifest.js` `MARK_KINDS` + validator, a `resolveConstructionMarks` dispatch branch in `neo-rembrandt/index.js`, a `sketch-vocab/arabesque.md` card (reindexed), and the mark documented in `mcp/tools/sketches.js`. Modes: `field` / `rosette` / `medallion`; deterministic, centred + scaled into the viewBox. Remaining enhancements (girih fivefold tiling, DCEL face extraction for perfect interlace, Bonner families) logged as non-blockers. Note: the running MCP server caches the mark enum — new mark needs a dev-server restart.
5. **Lit-body channel — killing the lathe "onion" across the view tree (DONE 2026-07-09).** Plan: [scene/lit-body-channel.plan.md](../control/lib/graph/scene/lit-body-channel.plan.md). What started as the orbit-view planet swap generalized into a reusable **`planets` runtime channel** in `scene/channels.js`: any spherical body → a real lit `THREE` UV-sphere (procedural texture-free surface: `tint`/`bands`/`mottle`; `star:true` = self-luminous + a `PointLight`; `opacity` shells; `spin`), built at the origin and registered into the shared `meshes` map so movers still bind + orbit it. Swept **nine views**: orbit (sun + planets, the pilot), cellular (cytoplasm envelope / nucleus / vacuole), mechanics (focal bodies in projectile / pendulum / collision / lever), comet (Sun → star), parallel-transport (translucent globe), reactor + cascade (nuclei / neutrons / fragments / flashes, mover-bound), fluid (Stokes / spin / rotor-sail balls), atom (nucleus). Non-sphere and secondary geometry stays faces. All shared snapshots regenerated; each view visually verified via WebGL PNG bake; 1007 tests green.
6. **figure-animal → sketch graduation (PLAN, not started).** Plan: [polygonizer/figure-animal-graduation.plan.md](../control/lib/graph/polygonizer/figure-animal-graduation.plan.md). The zoo-mammal kit (`figure-animal-*.js`, `ZOO_BUILDS`) renders correctly but only spike/unit tests reach it — nothing mints, persists, or serves an animal as an asset. This plans the four-part graduation (MCP tool + `stored-sketch-svg.js` kind-dispatch branch + routes + `TOOL_INDEX`/`ROUTING_INDEX` entry) that `create_figure` gave the protoform.
7. **Operations mode deprecated (LANDED 2026-07-09).** The Ring 11 domain-scoped deliberation surface was removed as unused — nothing consumed its descriptor/thesis semantics. Deleted: `lib/mcp/tools/operations-mode.js` (+ test, the 7 MCP tools), the `/operations` dashboard (`app/operations/page.jsx`) and its REST routes (`app/api/operations/*`). Unwired: the `registerOperationsModeTools()` registration in `server.js`, the Ring 11 routing block + "Scope a domain operation" row + Workshop-Home tile entry in `mcp/tools/context.js`, the nav tile (`workshop-nav.jsx`) + breadcrumb (`Breadcrumbs.jsx`), the dead `operations` i18n namespace in `messages/en.json`, and the now-dead `/operations` link on the `/motion` gallery. **Kept:** the `ops_tags` / `ops_tag_members` tables + [OpsTagRepository](../control/lib/db/repositories/ops-tags.js) — they remain motion's resource-grouping store (`forge_motion` / `stitch_motion` forge one tag per "Motion Project"; the `/motion` gallery groups by them via `listTagsForMember`). Nothing registers at Ring 11 anymore.

8. **forward_context orientation diet — the routing-card move (LANDED 2026-07-10).** The always-paid body had regrown to ~4.6K tokens (74% routing index; the Create-things section alone was 62% of the body, growing one near-ceiling row per creative capability — the per-row lint pinned row *size* but not row *count*). The Create-things section is now a **mini segmented index** (~1.4K tok routing index, ~2.5K tok whole body): one row per FORM (picture / object / world / motion / audio / game / publication) naming recognizers + entry tool; the full per-family rows (recognizer quotes + forks) retired into **routing cards** under [lib/mcp/routing-cards/](../control/lib/mcp/routing-cards/) (17 cards, JSON-frontmatter markdown + loader, `entry` field names the door), indexed as a new `routing` embeddings kind (same CHECK-rebuild migration) and returned *whole* by `semantic_search({kinds:['routing']})` — no follow-up reader. Guards: a **body-ceiling test** in context.test.js (11K chars — aggregate pin the row lint lacked), card lint (1,100-char body ceiling in the loader), an entry-tool registry sweep, and a **retrieval eval** ([routing-eval.integration.test.js](../control/lib/mcp/routing-cards/routing-eval.integration.test.js)): 18 paraphrased user phrasings → expected entry tool must appear in the top-3 cards against the REAL local ONNX embedder (auto-skips when the model isn't fetched). Adding a creative capability = a routing card + eval fixture rows, NOT a new body row. MCP-ARCHITECTURE §2's stale "~1.4K tokens" claim updated.

9. **Beats B8 — WAV export, the sound-sample seam (LANDED 2026-07-09).** Plan: [beats.plan.md → B8](../control/lib/graph/beats/beats.plan.md). A "sample" in mojulo is a beats ref: the recipe stays the source of truth and the WAV is a deterministic derived render (same manifest + options → the same bytes, verified byte-for-byte), export-only, never imported back — the `.glb` posture for audio. Three consumption tiers: the seam ([beats/beats-render.js](../control/lib/graph/beats/beats-render.js) — pure `renderBeatsPlan` reusing the kernel's event derivation + `renderBeatsOffline` replaying the kernel's own `createEngine()` on an `OfflineAudioContext`, + a dependency-free 16-bit WAV encoder) for server-side consumers; the URL (`/api/sketches/<ref>/beats.wav?bars|loops|cue|tail`, regenerates per request) for anything page-shaped; the tool (`export_beats`, the `export_model` contract — writes `control/data/exports/<ref>[.cue].wav`) for the host agent. Per-kind duration: composition = its own score length, ambient = `bars`, pattern = `loops`, sfx = one `cue` per render; 300s ceiling. One new native dep, `node-web-audio-api`, lazy-imported (create_beats/player never load it) and registered in `serverExternalPackages`. MP3 stays out.

10. **Beats plan closed — B4 remainder + B5.2 + B5.3 + B9 (LANDED 2026-07-10).** Plan: [beats.plan.md](../control/lib/graph/beats/beats.plan.md) (Status block updated; every phase now built). Four moves in one batch: **(a) B5.2 performance macros** — `transpose` (semitones, schedule-time) and `tone` (a low-pass at the chain HEAD so delay tails darken with their source, `toneFreq` maps [0,1] → 120Hz–18kHz) on every channel/part/track of the three musical kinds; kernel `setTranspose/setTone/setLevel/getMacro`, macro state survives stop/start, player sliders, offline-render parity (macro-less renders byte-identical). **(b) B5.3 world bindings** — `audio.bindings`: sim-state selectors (depth / height / speed / proximity; camera or entity subject) → channel macros, validated against the soundtrack at resolve time in [beats-world.js](../control/lib/graph/beats/beats-world.js), evaluated read-only per frame in the audio channel (capture never emits audio, so byte-identity holds). **(c) B4 remainder** — CSS3D `/scene` soundtrack (`emitSceneSoundtrackScript`, soundtrack + wind only, injected by `renderSceneHtml` on live views; the PNG rasterizer passes `capture:true` and bakes stay byte-identical), CLAUDE.md architecture-map entry, renderer-ladder Phase 5 trimmed to a pointer. **(d) B9 first-class beats** — sovereignty as a DOMAIN layer, rows stay in `sketches`: `beats_revisions` + `beats_annotations` tables + [repositories/beats.js](../control/lib/db/repositories/beats.js); four new MCP tools (`get_beats` / `update_beats` — full-manifest replace through the create gate, revision snapshot per edit, pre-B9 heads backfilled — / `annotate_beats` / `diff_beats` — structured musical diff accepting `ref@rev`); `update_sketch`/`diff_sketches` guard rails teach the domain tools; `exportsBaseDir` extracted to [tools/exports-dir.js](../control/lib/mcp/tools/exports-dir.js); the `/api/beats/<ref>` namespace (player `?rev=`, `<ref>.wav`, `/annotations` GET — annotation WRITES stay MCP-only via `annotate_beats`, `/meta`, `/diff`) with the `/api/sketches/<ref>/beats(.wav)` aliases intact; the **`/beats/<ref>` studio** (player iframe + revision list with "what changed" + read-only annotation panel + WAV export + copy-revision-prompt; `beatsStudio` i18n namespace in en.json — other locales pending a `/sync-locales` run); player grid + playhead + annotation markers for the pattern kind; `/maker/beats` cards link to the studio; TOOL_INDEX rows + an operate-flow ROUTING_INDEX row; vocab cards grew macro + revise sections. Tests: `lib/mcp/tools/beats.test.js` (B9 exits, incl. the diff-reports-exactly-the-kick-change pin), macro/binding/CSS3D suites in `lib/graph/beats/`, audio char-net fixtures re-pinned (intended — the kernel string changed). Addendum (same day): **MIDI export** — `export_beats { format:'midi' }` + `/api/beats/<ref>.mid` (`?rev=`) render the score as a format-1 SMF ([beats-midi.js](../control/lib/graph/beats/beats-midi.js), dependency-free, built on the same pure `renderBeatsPlan` seam as the WAV so swing/feel/velocities/transpose travel); GM program guesses, drums on channel 10, pitched foley skipped with a count, sfx refused, import stays out; studio Export .mid affordance. UI de-labeling (same day): beats stop being presented as sketches — `/sketches/<ref>` client-redirects beats kinds to the `/beats/<ref>` studio (the generic frame never renders them; API aliases untouched), the `/maker/beats` shelf gets beats-worded strings ("track", "Open Studio") via a `sketchesIndex.beatsNoun` override set, the studio gets a Studio → Beats → ref breadcrumb trail, and the "Open at /sketches" back-link + `/sketches/<ref>` mentions in tool copy were removed. Storage stays in `sketches` — the relabeling is surface-only.

11. **Workshop nav drawer — global navigation without going home (LANDED 2026-07-10).** New [components/workshop-nav.jsx](../control/components/workshop-nav.jsx) — the single source of truth for the three Workshop modes (**Ideate / Operate / Studio**) and their destination tiles (i18n keys + hrefs + line icons + per-mode hue vars in `globals.css`: indigo / teal / amber, cool→warm mirroring upstream→running→output). Rendered two ways: [HomeLauncher.jsx](../control/components/HomeLauncher.jsx) (slimmed ~430 lines to consume the shared model; its heading now reads "Workshop Home") and the new [WorkshopDrawer.jsx](../control/components/WorkshopDrawer.jsx) — a global left slide-out opened from the **AuthNav brand button** (the wordmark no longer navigates home; home is the first link inside the drawer), so the operator can jump anywhere without first returning home. The home page keeps the top bar again (the launcher's old Settings tile folded into the nav row); `home.drawer.*` strings in `en.json`; `Breadcrumbs.jsx` adjusted alongside.
12. **Zoo-mammals third pass — the plated snout + herd fill-out.** Plan: [polygonizer/zoo-mammals.plan.md](../control/lib/graph/polygonizer/zoo-mammals.plan.md) (checklist re-marked). New [polygonizer/figure-animal-faceplate.js](../control/lib/graph/polygonizer/figure-animal-faceplate.js) — a **plated snout**: a projecting tapered muzzle built as four plates (topL/topR tenting off a sloped dorsal ridge + nose cap + jaw plane) swept root→nose off a small cranium, so heads read as one clean form in profile instead of a lathe blob. Newly DONE recipes: cougar (long dark-tipped tail), hippo, rhino (stumpy + nasal horn), horse (nape mane + full tail), wombat; lion promoted to a first-class `ZOO_BUILDS` recipe. Koala **cut + logged, not faked** (reads as "small grey bear" without an upright tree-clinging stance lever). `figure-animal-antler.js` / `figure-animal-build.js` adjusted alongside.
13. **Fractal-city landmark budgeting fix.** [city/fractal-city.js](../control/lib/graph/city/fractal-city.js): `landmarkAnchor` gains a `sizeRegion` budget basis — when a corridor pushes placement into a side band, monuments keep their **real surface-area budget** off the full city region (displacing blocks) instead of being re-budgeted off the shallow band — plus an iterative **across-axis fit** (footprint + plaza ring must sit inside the band, never crossing a reserved boulevard) and no `baseScale` pre-shrink.
14. **TERMS.md legal pin.** The maintainer is now named (**2364758 Ontario Inc.**, an Ontario corporation) and governing law pinned to Ontario + applicable federal Canadian law, replacing the "jurisdiction in which the maintainer resides" placeholder.

Riding along: `vehicles/vehicles-swept.js` + `polygonizer/vehicle-swept-net.js` tweaks, the game-ui-language char-net re-pin in `scene/__snapshots__/emit-channels.char.test.js.snap`, [beats/beats-resolve.js](../control/lib/graph/beats/beats-resolve.js) (the shared `?rev=` ref/revision resolution seam behind all beats HTTP surfaces, part of item 10's B9), the CLAUDE.md architecture-map entry for beats, and the DB/embeddings schema tweak (`lib/db/index.js`, `repositories/embeddings.js` — the `meta_embeddings` CHECK now also carries `game_kit` / `game_glyph` / `game_sfx` / `routing` source kinds behind the same CHECK-rebuild migration).

**Implication for a new agent:** the changelog's tool inventory is stale for the view family — do not look for `atom_view` / `black_hole_view` / etc. as individual tools; they are kinds under `create_view`. When touching this branch, keep `TOOL_INDEX` / `ROUTING_INDEX` in context.js in sync (the registry-sweep test will catch drift). Same posture for the World emitter: a new runtime channel is a `RUNTIME_CHANNELS` row in `scene/channels.js` (plus an emit-fixtures entry), NOT hand-wired splices in scene-three.js — and any change to emitted bytes fails the char-net snapshots, which may only be re-pinned when the change is intended (kernel edits count: the beats/physics/controllable kernels are stringified into pages).

## 7. Invariants a new model must not violate

- Conversation data never moves into the control-plane DB; per-bot reads go through the bot proxy.
- All builder paradigms converge on `buildDeploymentConfig()` — no paradigm-specific branches downstream.
- Bot turns go through the hashing helpers; never bypass `content_hash` / `chain_hash`.
- The bot image is bot-agnostic; Fly injects per-bot config as files. Pin exact bot tags, never `:latest`.
- `forward_context` stays a thin routing index; heavy orientation lives behind drawers.
- MCP transport binds to localhost only; no tunnels, no auth layer assumed.
- The dashboard is not a conversational surface (the bot builder chat is the deliberate exception).
- Never `cat`/`Read` a `.env` under `$MOJULO_HOME` or in an unzipped bot — use `inspect_bot_env`.
- No intent-classification / use-case gating layers; capability and suitability assessments belong to the operator ([responsibility-model.md](responsibility-model.md)).
- Synthesize ≠ certify: dry-run on one real input → inspect → promote. Re-validate on schema drift.

## 8. Design signature (the taste that should carry into new work)

- **Progressive disclosure everywhere**: thin index → drawers → vocab cards; context cost is paid on demand, never front-loaded.
- **Recipes, not renders, in the DB**: tiny deterministic manifests regenerated on render (a painted landscape is ~30 tokens vs ~14K of raw SVG).
- **Append-only deliberation separated from replaceable present-state**: contextmap (why, sealed) vs inventory (what's installed, replace-semantic) vs outcomes (what fired, never written to contextmap).
- **The agent is the inference and vision layer**: apps park inference on the queue; photo references are read by the agent's own eyes; cooks are agent-authored. Mojulo needs no keys on those paths.
- **Closed vocabularies + seeded determinism** for generative surfaces: glyph cards, heartbeats/splatches, view-vocab — content-extensible by dropping a card, code-extensible only for new families.

## 9. How mojulo accreted (origin & stance)

None of the architecture above came from a roadmap. It accreted, each layer built exactly deep enough to unblock the next — and that history explains the shape better than any spec would.

**The chain.** Mojulo began as an enterprise/sovereignty play, disrupted by frontier-lab capability announcements; the author built in stealth and pivoted continuously before open-sourcing the initial chatbot layer. An MCP experiment then unlocked the connected-services platform → **metacontext** became necessary so composed tools wouldn't sprawl or repeat → which extended naturally to vibe coding, then spec writing → mojulo was already Node.js, so a **runtime** followed → then the idea of the agent writing to the app and harnessing the filesystem + event bus → the system got complicated enough to need a **chart-rendering tool to depict itself**. Then, for reasons unknown, the author applied two-point perspective, draughtsmanship, and mandala principles to fake perspective with divs and CSS → **pure-world**: all vector, no pixels, map everything there first → frame-by-frame animation (each shot is a moment in time; frames stitch to GIFs, GIFs stitch to movies) → the **figure** → three.js made worlds walkable → live objects moving in code brought in **physics** via experiments → which accreted into the science explainers, culminating in the black-hole viewer → and Newtonian physics was clawed back into the world renderer and its own event bus.

**Stances a new model should hold:**

- **No onboarding is the product.** The coined lexicon isn't a tax to be mitigated — the ask-and-discover loop is the intended interface. You ask what it can do, it gives you clues, and agent + operator build something interesting together. The bug class to watch for is *dead-end clues* (terms that don't reward the question), not unfamiliar terms per se.
- **The frontier moves; layers get second passes on demand.** Surfaces held at Phase-1 depth are not debt — each was built to unblock its successor. A layer deepens when something downstream demands it (physics → world renderer is the exemplar). Per task, ask: is this layer still at the frontier, or has something started demanding its second pass?
- **Pure-world (all-vector, no-pixels) is the load-bearing constraint** of the entire visual arm. It's what keeps recipes tiny, makes animation stitching rather than rendering, makes diffs structural, and makes real-unit read-back (`measure_view`, SI-honest experiments) possible. New visual work should keep the world pure-vector.
- **Happy to fail.** The visualization shift adds positioning complexity *and* a unique shot at enabling users to one-shot something nobody has done. Weight advice toward "does this make the one-shot better and the failure legible/cheap," not conventional prudence. The two arms (control plane, visual substrate) should feed each other — the `operator` world base (mojulo rendering its own state as a walkable world) is the synthesis pattern.
- **The self-depiction loop is a compass.** Twice now, tools built to depict the system's own complexity have become product (chart rendering → Ring 10). When guessing what accretes next, trust that engine.
