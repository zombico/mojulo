# Mojulo — Status Check & Orientation Report

**Date:** 2026-08-07 · **Branch:** `main` (**v1.0.0 — merged and tagged**). The visualization-era batch plus the 2026-08-06 late-run batch (controllable-world decomposition, drivable/flyable vehicles, orientation containment C1–C3, fog `depthClip`) are all committed and merged to `main` via a `--no-ff` release merge, tagged **`v1.0.0`** (previous highest tag was `v0.8.0`). Working tree clean. See §6 for the release contents. **Patched by `v1.0.1` then `v1.0.2`.** `v1.0.1` re-pinned ten stale controllable emit-channel characterization hashes (test-only). `v1.0.2` then fixed the real break: 1.0.0's controllable-world decomposition had statically imported three modules out of the gitignored mobile-suit pack (`ms-maneuvers`, `ms-ai`, `agent-commander`), so a clean checkout (CI / `npx`) threw `ERR_MODULE_NOT_FOUND` at module load. Resolution — cull the agent-spectate "make-an-agent-play" spike from tracked source (byte-neutral; it was gated off), and track the two engine-behavior modules (`ms-maneuvers` / `ms-ai`) directly via a `.gitignore` exception while the rest of the content pack stays ignored. Green in both pack-present (424/6134) and pack-absent (412/6025) states. Then `v1.0.3` made CI itself green: the visualization-era characterization snapshots hashed full-precision trig floats pinned to the dev machine (node 24/macOS) that drift ~1 ULP on CI (node 20/glibc), so CI had been red the whole time — quantize floats before hashing (world-scene.kinds → 8 sig figs, controllable traces → 6) + fix a time-seeded session-token flake. `v1.0.3` is the stable green tag and npm `latest`. **2026-08-10:** the branch-state batches below are committed together on **`visual-layer-enhancement`** (unmerged to `main`; the release draft is [control/CHANGELOG.md](../control/CHANGELOG.md) §[1.1.0] — tag/merge is a separate, deliberate step).
**Branch state 2026-08-08 (committed 2026-08-10 on `visual-layer-enhancement`):** the **motion-comic M0** batch is in the working tree — a new creative FORM (`motion-comic`, the 12th in `CREATIVE_FORMS`): the click-gated comic presentation ("the powerpoint of comics" — a fixed BOX + matte; **no pages — bound by panels**: each scene frames one PANEL CROP (`{pageRef, panel}`) as the showcase and its events piecemeal balloons/elements out one click at a time; scene `layout` regions (`full-spread` bleed / `splash` 80% showcase / `two-panel`, slot-placed and aspect-fitted at mint); the `swap` delta = "direct single panel action" (same frame, new art, cut-only default — closure interpolates; the infinite-comic paradigm) and the `move` delta (the camera cheat: shrink = depth, grow = approach); the `letter` delta (SFX lettering as mojulo-drawn z-index overlays — the image worker never letters; deterministic handwritten-bubble glyphs, slam-in, anchored to placements); the trick protocol (`motion-comic-tricks` vocab card — the keyframe cheat shelf adopted as vocabulary, now merged with the researched **Turbomedia/Balak grammar**: `hold` held beats, `focus` rack focus, balloon `tail` foreshadow pointers, build-and-collapse / black-frame / match-cut recipes; posture sealed as "the powerpoint is the means, not the end"); state as a pure fold, `bubbles` vs movie-style `subtitles` lettering, four-move navigation, one-file HTML export). New: [control/lib/graph/motion-comic/](../control/lib/graph/motion-comic/) (manifest + resolver + player emitter + tests + plan), `/api/sketches/[ref]/play` route, kind plumbing (`sketch-manifest.js` render mode `play`, bucket `illustration`; `create_sketch`/`update_sketch` dispatch), FORM row (creative-forms.js + FORM_TOOLSETS + studio routing row + routing card + sketch-vocab card), studio body ceiling consciously grown to 7 750. Design + resolved operator decisions: [control/lib/graph/motion-comic/motion-comic.plan.md](../control/lib/graph/motion-comic/motion-comic.plan.md).
**Branch state 2026-08-09/10 (committed 2026-08-10 on `visual-layer-enhancement`):** **Mobile Suit Arena simplification, spectate picks, publish pipeline, and gameplay tweaks** (the game is published from `data/outcomes/` to GitHub Pages; the mojulo source changes stay uncommitted). (1) The **Agent Duel** mode (0807 agent-spectate spike affordance) is removed from the game UI — the 5 `watch_agent` level builds + the `sc_agent` menu entry are gone from [mint-arena-game.mjs](../control/lib/graph/mobile-suit/scripts/mint-arena-game.mjs) and the re-minted `sk_ms_arena`; the engine's opt-in `agentSpectate` plumbing (agent-commander.js, emit-side port) stays, unexercised. The 5 orphaned `sk_ms_arena_watch_agent_*` level rows remain in the DB, unreachable. (2) **AI Spectate casts are now the operator's pick**: `buildWatch` authors every candidate seat and the launch params despawn the unpicked — solo watch = two hangar pages (fighter A / fighter B, a full `wa_`/`wb_` clone rail per side so any suit can face any suit, mirror matches included); team watch = per-side roster check-picks (`watch_blue`/`watch_red`, up to 3 each); ffa watch = a `watch_cast` check-pick (everyone fights by default). Plumbing: new setup style **`roster`** (named member check-pick, default-checked from the level's `presets.default`) in [game-manifest.js](../control/lib/graph/game/game-manifest.js) + [game-shell.js](../control/lib/graph/game/game-shell.js) `renderRosterPick`; watch contracts carry `spectate: true` so the shell's difficulty card stays off all-AI setups; the game-params seam ([channels/controllable/index.js](../control/lib/graph/scene/channels/controllable/index.js) `__applyMatchParams`) grew a spectate branch that despawns unpicked `seat:'ai'` fighters (was a hard no-picks return; empty/absent picks still keep the whole cast). Emit-channel characterization snapshots re-pinned (established v1.0.1 workflow). (3) **`export_game` shared figure bank** ([export-game.js](../control/lib/mcp/tools/export-game.js)): a suit-roster level page was ~98% inlined `__FIG` (the packed rigged-figure bank) repeated identically in every level/preview page — 149MB/page, ~5GB/export, past GitHub's 100MB/file hard limit. The export now hoists the bank out of each emitted page into content-hashed shared `assets/figures/<name>-<hash8>.json` files (string-level span scan, no full parse; deduped across all pages) and replaces the literal with a top-level-await fetch shim. Arena export: 149MB → ~4MB per level, 901MB total, no file over 20MB; browser caches suits across levels. Trade-off recorded in tool description + README + the `get_tool_index` drawer copy: exported folders now need an HTTP server (file:// no longer loads levels). First consumer: **Mobile Suit Arena published to GitHub Pages** — repo `zombico/mobile-suit-arena`, played at `https://zombico.github.io/mobile-suit-arena/`. (4) **Arena gameplay tweaks** (2026-08-09/10, live locally, deploy held): NEWTYPE tackle-guard **facing gate** ([ms-ai.js](../control/lib/graph/mobile-suit/ms-ai.js) — the counter only fires on a swing inside the tackler's front cone, `tackleGuardCone` default 1.1 rad; no more 180° whip-tackles; tests in ai-tackle-facing.test.js); AI **maneuver solidity** (dodge-roll + tackle-charge playbacks now run the same `resolveBlocking` pass as the pilot's dash — a NEWTYPE body-check could phase through the depot's 16-unit interior wall; ai-maneuver-blocking.test.js); **charged beam-rifle range** ([combat-ranged.js](../control/lib/graph/worlds/controllable/combat-ranged.js) `chargedRangeMul`, default 1.2 — a full mk2/taisa charge carries 20% further, per-weapon opt-out); **geof HEAT ROD re-hand** (the whip is a right-hand weapon on the standard mk2-saber swing set for all cardinal verbs — hidden handle, no racks, the lash/dart strike forms whip out of the right fist mid-swing; assembler items re-anchored `_hand_l`→`_hand_r` in sk_geof_unit_v1_multi(+_ground_type), 56 stored manifests patched in-place convergent with the mint-script edit; eyes-gated via whip-rehand.spike.gen.test.js, superseding the culled whip-verbs spike); **taisa buffs** (`chargedDamage` engine knob — the stun bolt lands 3.5 bullets' worth, 42 vs 12/round; hp 850 → 1000 across 96 bodies + the hangar card); **HUD**: auto weapons (gatlings) no longer flash the reticle cooldown ring (the between-round gap IS the fire rate); **opt-in jet families** (`manifest.jets` in [unit-rig.js](../control/lib/graph/worlds/unit-rig.js) — 'calf' rear-leg nozzles for taisa, 'torso-back' vectoring pack pair for the mk2 whose integrated pack had no named thruster station and boosted flameless; geometry-derived, byte-identical absent; eyes-gated via jets-optin.spike.gen.test.js; the mk2's flames were then re-anchored to its two authored pack-cone mouths via `manifest.jets` OBJECT entries — model-space `{bone, anchors, dir}` mapped through the assembly rotation); **mk2 shield re-grip** (the folded shield's rest geometry draped across the chest gripped at its top corner — all 8 `shield_forearm_l_*` plate anchors translated so the shield's center mass sits in front of the LEFT fist, measured via an unscaled bakeUnitRig probe and eyes-gated with a spectate front-view render; preview worlds hold stale item copies until the next suit-card re-mint); **beam recolor** (mk2 V78 → pink 0xff5fd0, taisa → green 0x4dff70, 49 manifests patched). New menu art (menu-art.png, operator-supplied; previous kept as menu-art-v2.png).
**Branch state 2026-08-10 (committed on `visual-layer-enhancement`):** **Arena game-mode abstraction** — the mode twin of the earlier suit abstraction (`unitRef`), landed in three proven-equivalent steps. (1) **The mode registry** [mobile-suit/arena-modes.js](../control/lib/graph/mobile-suit/arena-modes.js) (+ arena-modes.test.js): the five mode builders (`buildSolo`/`buildPractice`/`buildFfa`/`buildTeam`/`buildWatch`), their shared helpers (`arenaGeometry`/`seatRule`/`matchLayer`/spectator camera) and the bout constants extracted VERBATIM from [mint-arena-game.mjs](../control/lib/graph/mobile-suit/scripts/mint-arena-game.mjs) into an `ARENA_MODES` registry (each entry: builder + menu placement), `composeArenaLevel(modeId, mapSrc, opts)` the one dispatch. The mint script shrank to enumeration: `LEVELS` and the two-step menu are now DERIVED from `MAPS × ARENA_MODES` — **adding a map is one row in MAPS (every mode reaches it); adding a mode is one registry entry (every map gains it)**. Extraction proven a no-op by dry-run content hashes (new `mhash` printed per row): all 62 rows (35 levels + 26 card sketches + gameSpec) byte-identical pre/post. (2) **`mapRef` terrain inheritance** — [worlds/map-ref.js](../control/lib/graph/worlds/map-ref.js) `applyMapRef` (spread-merge, level wins wholesale, `null` = tombstone deleting an inherited key) resolved at the TOP of [worlds/world-scene.js](../control/lib/graph/worlds/world-scene.js) `resolveWorldScene` (controllable kinds only; absent ⇒ byte-identical), so /world, /model.glb, export_game and the audit runner all inherit through the one seam. `lightenArenaLevel` strips a built level to the mode's own output (`entities`/`figures`/`match`/`game`/`camera`/`shadows` + tombstones for the map's pinned `ai:'off'`/`pilot`/`note`) + `mapRef`. [export-game.js](../control/lib/mcp/tools/export-game.js) ships the referenced map recipe beside a light level's recipe (deduped) so `recipe/` stays re-mintable. (3) **Re-minted the 35-level matrix in light form** — each level row now stores ~46KB of mode output instead of a full terrain clone: `sk_ms_arena_*` rows **80.6MB → 19.8MB** (the remaining weight is the untouched legacy free-play/spike rows; the 35 matrix rows total 1.59MB, was ~78MB), and a map source re-mint now flows terrain to every mode variant without re-running the game mint (roster/loadout changes still re-mint — seats snapshot `body`/`rule` at build; seat-by-reference is the staked follow-up). Gates: 35/35 cells `merge(JSON(lighten(full))) ≡ full`; resolved `/world` payloads for 3 sample levels (ffa_ground / team_colony / watch_team_space) **byte-identical** pre/post re-mint; game row updated in place through `resolveGame` contract validation; live smoke on the dev server (game shell + 3 light-level world pages 200, figure bank present); suites green (lib/graph/mobile-suit + worlds + game: 839 tests; lib/mcp/tools: 817). Pre-remint DB backup in the session scratchpad. The post-mortem's generalization threads are staked in [worlds/match-modes.plan.md](../control/lib/graph/worlds/match-modes.plan.md) — and **M1–M4 landed the same day**: (M1) `mapRef` accepted/validated at the controllable mint + taught in the controllable view-vocab card and the game mechanics-guide card; (M2) `export_game` hoists big `GROUPS`/`REPEATS`/`TEXTURES` literals into content-deduped `assets/geometry/` files (mode variants of one map ship its terrain once); (M3) the suit-agnostic mode core promoted to the tracked engine as [worlds/match-modes.js](../control/lib/graph/worlds/match-modes.js) (five builders + geometry/seat/match helpers + `lightenMatchLevel`, `prelude`/`nameOf` pack hooks; `arena-modes.js` shrank to NAMES + two preludes + the registry; all 35 arena dry-run hashes byte-identical to the stored rows; `worlds/match-modes.test.js` is the pack-absent tripwire); (M4, operator decision: extend compose_world, zero tools/list cost) the controllable base takes `match: { mode, killTarget?, rivals?, teamNames?, … }` + `mapRef` — ANY stored controllable world with pilotable entities mints as a solo/practice/ffa/team/watch match level in light form, promotable via `create_game` (gate test mints ffa + watch over a toy map, promotes, resolves). `KNOWN_RULES` grew `'ai'`. M5 (seat-by-reference) + M6 (derivation chains) stay open in the plan. Suites at the boundary: 2,140 tests green across worlds/mobile-suit/game/views/mcp-tools.
**Branch state 2026-08-09/10 (committed on `visual-layer-enhancement` — the substrate threads beside the arena work):** five threads, all additive (absent the new opt-ins, existing recipes render byte-identical). (1) **Dungeon world kind — interchange I0**: `kind:'dungeon'` wired into [worlds/world-kinds.js](../control/lib/graph/worlds/world-kinds.js) (`assembleDungeonScene` from [architecture/dungeon-designer.js](../control/lib/graph/architecture/dungeon-designer.js); `walk: true`, no CSS-3D /scene form), `compose_world` base `dungeon` (identityAdapter; structural validity — a tunnel to an unknown chamber, an unknown material — throws at mint; the movement-flow check is advisory, never gated), mint in [mcp/tools/scene-dungeon.js](../control/lib/mcp/tools/scene-dungeon.js) (+6 tests), parameter manual in the [dungeon view-vocab card](../control/lib/graph/views/view-vocab/dungeon.md); `.glb` export eligibility followed free from registry membership; `compose_world` description re-pinned 1488 → 1580. Staked from the same session: **[interchange.plan.md](../control/lib/graph/interchange.plan.md)** (PROPOSED, I0 landed) — "standard formats at the edge, recipes at home": I1 animated GLB (the rigid-FK packed clips decompose exactly into glTF node-TRS samplers — no skins, one exporter for figure/unit/vehicle rigs), I2 eligibility widening (figure, carved-solid), I3 the bind-back door (`bind_mesh_render` + `meshRef` body source), I4 level-as-layout semantic GLB; blender-mcp is the standing verification rig (machine gate = scripted `bpy` introspection, eyes gate = viewport screenshot), which killed the mesh-worker queue in v1. (2) **Key-free polygonizer handoff**: `get_polygonizer_packet` / `submit_polygonizer_manifest` in [mcp/tools/sketches.js](../control/lib/mcp/tools/sketches.js) — the agent-as-model twin of `create_polygonized_sketch`. The packet hands the calling agent the same system/user prompts + schema the keyed path would send a provider; submit runs the model-independent tail (glyph implications → validate → deterministic repairs → lower → mint) via new `finalizeAgentManifest` / `finalizePlanningManifest` exports from [polygonizer/index.js](../control/lib/graph/polygonizer/index.js); a failed submit returns `repairPrompt` — the server-side maxRepairs loop becomes conversational. Plan-then-skin passes the planning manifest back into submit and the scaffold is recomputed server-side, never trusted from the agent. Tests: [polygonizer-handoff.test.js](../control/lib/mcp/tools/polygonizer-handoff.test.js) + index.test.js growth. `PAYLOAD_CEILING` re-pinned 376,000 → **381,000** (measured 379,549). (3) **Baked coverage shading** (what named the branch): [effects/ao-bake.js](../control/lib/graph/effects/ao-bake.js) gains **`bakeSkyShadow`** — the DIRECTIONAL sibling of the AO bake (per-corner overhead rays, Möller–Trumbore over grid-culled candidates, a small cone fan tilted toward the face's open side so vertical surfaces under outboard overhangs shade too; distance falloff; MULTIPLIES into existing `vao`), with its own LRU (`SKY_CACHE`) and `AO_CACHE_MAX` 16 → 32 (a map bake must not evict a 14-suit roster mid-session) + a `vao`-aware cache key. [worlds/unit-rig.js](../control/lib/graph/worlds/unit-rig.js) `bakeUnitRig` gains opt-in **`ao:`** — crevice AO × sky shadow over the whole rest-pose body folded into per-part vertex colours (the bicep under a pauldron, the thigh under a skirt plate; pose-invariant by intent, zero runtime cost; weapon/shield kit excluded both ways; absent ⇒ byte-identical bake). The cast-shadows channel gains **`shadows.cast.noCastGroups`** ([channels/cast-shadows.js](../control/lib/graph/scene/channels/cast-shadows.js)): a roof/ceiling group RECEIVES but never CASTS, so an enclosed interior no longer blankets its floor in the sun's shadow — emitted only when declared. New [materials/face-material-roles.js](../control/lib/graph/materials/face-material-roles.js) (+tests): a pure manifest→manifest selector stamping procedural-material presets onto a finished map's faces by height band or baked fill colour (roles selector staked for when generators tag roles); procedural-material's vexar import went relative so mint scripts compose it under plain `node`. (4) **Polygonizer house/floorplan pass**: [recipe-compiler.js](../control/lib/graph/polygonizer/recipe-compiler.js) re-authors the whole house assembly in ONE compiler-owned oblique space (facade rect as shared datum, depth recedes by `proj.dx/dy`, porch/steps apron by `proj.fwd`; no generic `solid` path — solids re-anchor under the renderer's camera, which is what detached the roof from the body); [floorplan-structure.js](../control/lib/graph/polygonizer/floorplan-structure.js) entry-door/furnish upgrades; [sketch-manifest.js](../control/lib/graph/sketch/sketch-manifest.js) gains the kind-dispatch gate so `create_sketch` can MINT the seeded interior kinds (`floorplan`/`restaurant`), not just render them. (5) **README**: npm/license/node badges + the `npx mojulo init` install one-liner above the fold. Known red at the boundary: [tool-descriptions.test.js](../control/lib/mcp/tools/tool-descriptions.test.js) carries one unblessed failure — `export_game`'s description grew to ~1411 (pin 1253) with the shared-figure-bank copy; blessing it belongs to the arena/export batch commit.

**Branch state 2026-08-10 (committed on `visual-layer-enhancement`, arena level-load performance):** a level load was ~40–55s server bake + a 148.5MB page EVERY play; profiled (55% of CPU in `bakeSkyShadow`'s `rayNear` — the suit `ao` sky-ray fan × 14 roster figures; 145MB of the page was `__FIG`, with every livery variant a byte-identical geometry duplicate) and fixed on five fronts. (1) **Suit `ao` retired from the default arena stamp** — [arena-compose.js](../control/lib/graph/mobile-suit/arena-compose.js) `stampSuitMaterial` default is now `{ contrast: true }` (explicit `ao` still rides through; a replacement shadow treatment is the operator's follow-up); the 35-level matrix + 26 card sketches re-minted, figure specs carry contrast only. (2) **Rig-bake LRU** — [worlds/unit-rig.js](../control/lib/graph/worlds/unit-rig.js) `bakeUnitRig` is now a keyed cache (RIG_CACHE 32, manifest+opts hash; function-bearing `clips`/`rig` overrides bypass; hits return shallow-protected copies so `previewClip`/`weatherRigParts` can't corrupt entries) over `bakeUnitRigUncached` — every one of the 35 levels re-bakes the SAME roster, so level bakes after a roster's first are ~1–1.5s (measured: solo_city 1.25s cold, practice_ground full rebake 1.0s; a fresh roster VARIANT — the space clip set — bakes once at ~29s). (3) **Figure-buffer pool** — the controllable channel ([channels/controllable/index.js](../control/lib/graph/scene/channels/controllable/index.js)) dedupes packed buffers ≥256 chars appearing ≥2× across `__FIG` into `__FIGPOOL` + in-page `__unpool` marker rehydration (livery variants share pos + clips; arena page 148.5MB → 97.5MB, −34%); a figures map with no duplicates emits byte-identical (char pins green unchanged). (4) **/world browser cache tier** — the route now sends `ETag` (= the server world-cache key hash) under `no-cache`; a matching `If-None-Match` 304s BEFORE any resolve work (measured 17ms, 0 bytes — replays skip bake + transfer + parse even across server restarts); `?nocache=1` stays `no-store`; `WORLD_CACHE_BUDGET` 512MB → 1GB, env-tunable `MOJULO_WORLD_CACHE_MB`. (5) **Level pre-warm** — the game shell's boot oven queue ([game-shell.js](../control/lib/graph/game/game-shell.js) `warmSetupPreviews`) now warms LEVEL worlds after the suit previews, one at a time, using the exact launch URL so the cache key + ETag match the play request. (6) **Indexed + quantized rig parts** (same batch, second pass): unit-rig parts were unindexed float32 triangle soup (~10MB/suit b64). [rig-bake.js](../control/lib/graph/figures/rig-bake.js) `packRigMesh` now packs positions on a per-part uint16 grid (`o` origin + per-axis `s` step; worst-case error 1.3e-4 world units, verified), vertices deduped on the (quantized pos, colour, spec) tuple, triangles as a uint16/uint32 index, and spec as `spec8` u8 pairs ([strength×255, shininess] — spec was the single biggest field of a contrast page at 19MB). Self-describing per part: the runtime's `__makeRigGroup` takes the `part.q` branch (gated on `figHasQ` so rig pages without unit rigs stay byte-identical); vehicle/protoform rigs still pack legacy `pos` soup; `weatherRigParts` reads both via the exported `partPositions`; export_game additionally hoists the `__FIGPOOL` literal into the content-hashed figure bank (without which the pool would have shipped inline per page). **Net level-load results:** page 148.5MB → 47.7MB (−68%); first roster bake ~40–55s → ~20s cold process / ~1.5s per suit; every subsequent level ~0.6–1.5s; replay 304 in ~10ms. Emit-channel char pins re-pinned (established workflow); unit-rig/ms-contrast shape tests updated to the q form. Verified: 1,224 tests green across scene/worlds/game/materials/figures/mobile-suit/mcp-tools, ai-battle-spectate spike green in real Chrome (236s, 3/3 — decoder + unpool + spec8 + engine in one pass), headless-Chrome screenshot of the live level eyes-clean (no cracks/inversions, livery + shield + HUD correct, zero page errors), index integrity + quantization bound checked numerically over all 21 g_multi parts.
**Branch state 2026-08-10 (committed on `visual-layer-enhancement`, Tutorial Mode):** the arena gains a scripted TUTORIAL track ([tutorial-mode.plan.md](../control/lib/graph/mobile-suit/tutorial-mode.plan.md), T1–T4 all landed same-day). (1) **The tutorial director** — a new engine system [worlds/controllable/tutorial.js](../control/lib/graph/worlds/controllable/tutorial.js) in `EMISSION_ENGINE` (match-layer idioms throughout: `registerStateInit` spec.tutorial → state.tutorial, a `tutorial` world pass at order 60 after `match`, self-quarantining `dead` flag): an ordered `steps:[{ id, prompt, goal:{fact,count?,of?,dir?,slot?}, activate?, set? }]` script advanced by **edge-detecting live entity state** each frame — zero emissions added to the combat systems, byte-identical absent. Facts: move · ascend/descend (vertical units traveled — the space thrust lessons, stashed-z idiom) · boost · boost-steer · jump · hit · clip-empty · switch · swing(dir) · combo (the follow-up connect) · tackle · dodge (the maneuver facts edge-read the engine commit counters `e.tackleCount`/`e.dodgeCount`, so the director stays pack-agnostic) · stagger · destroy; a destroy `of` may name an id SET (`of:[ids]` — all must fall); a softlock guard self-completes any step whose named target(s) are already down. Steps' one-shot entry actions: `activate` (wake dormant seats) + `heal` (restore a body to its normalize-hoisted `hpMax`/`shieldMax` — the between-phases repair; `'pilot'` resolves to the piloted seat) + `set.aiDifficulty` (retune the world brain mid-level, late-bound `E.AI_DIFFICULTY`). The director is the level TERMINAL (tutorial levels run no match): pilot downed ⇒ fail — unless the active step is **`optional: true`** (a BONUS phase: the clear was earned before it entered, so goal-complete AND pilot-down both end in success); last step ⇒ success. Two sibling affordances in the same file: the **`patrol` rule** (passive waypoint walker — deterministic turn-then-march loop with the ai's head-height ground snap; hittable/staggerable/downable, never targets, never fires) and **dormant seats** (`dormant:true` — hidden via `gone`, unhittable, rule suppressed by a `dormant` body owner at order 1, never enrolled by the match layer; `wakeDormant` returns it with the R24 sky drop-in + wake i-frames — the deterministic mid-level reinforcement; no runtime spawn system). (2) **Channel + threading**: the controllable channel ([channels/controllable/index.js](../control/lib/graph/scene/channels/controllable/index.js)) grew the objective HUD (`__updateTutorialHud` — top-center prompt card with progress ticks + advance flash, completion banner, and the one outcome envelope via the same contract-driven promote/end discipline as the match path; floating enemy-HP bars now skip `gone` bodies so dormant seats and vanished wrecks never float a bar); `tutorial` + `aiDifficulty` (mint-time tier seed) thread manifest → world-scene → emitThreeWorld → createWorld and join `LEVEL_OWN_KEYS` so light levels carry them; pipeline pin + emit-channel char pins re-pinned (established workflow). (3) **The three levels**, composed by [mobile-suit/tutorial-levels.js](../control/lib/graph/mobile-suit/tutorial-levels.js) (hand-authored casts in the mode-builder mold — same prelude, so the dummy's "green Z-A11" is just the z's default zeonic livery) and enumerated by the game mint after the matrix: **`sk_ms_tutorial_rising`** ("Mobile Suit Rising", City — the mk2 trimmed to HEAD VULCAN + BEAM SABER, no shield, vs a `patrol` Z-A11 at 350 hp; 13 steps: WASD → boost → boost-steer → jump → vulcan hits → clip dry → switch → left/right cuts → 2-hit combo → downswing → tackle (SHIFT — the prompt warns it dumps the whole thruster) → destroy) and **`sk_ms_tutorial_canyon`** ("Canyon Rumble", Canyon — full mk2 kit vs an armed Z-A11 at minted `aiDifficulty:'easy'` (RECRUIT; a consume-less contract renders no shell difficulty card, so the pick can't override the lesson tier); teaches switching + staggers, then the drop-in step wakes the dormant `en_geof` at ACE with its own goal a DODGE ROLL — "TAP F TWICE WITH A DIRECTION HELD" — so the player learns to evade while an ace hunts them, before "BRING HIM DOWN" runs as its own step; then the **BONUS WAVE**: Geof falling triggers "ENEMY MOBILE SUITS DETECTED! DEPLOYING BACKUP" — full hull+shield repair for the pilot (`heal:['pilot']`), a V78 ally waking beside the player + three TORETTOs on the far side (a 0.8-frac ring at the angle opposite the player; all four dormant seats on one `activate`, so the drop-ins ARE the deployment), sides picked without a match layer by `team` tags ('blue'/'red') + the ai's team-aware `target:'enemy'` hunt; the step is `optional` — wipe the squad (`of:[3 ids]` destroy) or go down swinging, the level clears either way); and **`sk_ms_tutorial_space`** ("Encounters in Space", the asteroid-field station map, `aiSource: ground` for donor brains like the arena's space cells — the full-kit mk2 in 6DoF; teaches thrust (WASD) → ascend (SPACE) → descend (X) → boost (F) → the SPACE ROLL (tap-tap-F), all enemies dormant through the lessons; then "HOSTILE CONTACT" wakes one green Z-A11 (destroy it — the mission objective), and its fall triggers the optional wave: "ENEMY MOBILE SUITS DETECTED — A TAISA AND HIS ESCORT. DEPLOYING BACKUP. OPTIONAL: WIPE THEM OUT", a taisa + two more green z clones on a wider ring at `set.aiDifficulty:'medium'` plus a V78 backup deploying beside the player (Canyon's team pattern — blue player+ally, red hunters via `target:'enemy'`; the first-contact z stays a teamless 1v1), no heal; space missions carry NO `shadows` key via prepTutorial's `space` flag, mirroring the arena space cells). The maneuver lessons + both optional waves + the director affordances they demanded (`heal`, `optional`, id-set `of`, ascend/descend facts) landed 2026-08-10 by operator ask, along with unlocking + numbering the missions ("1. Mobile Suit Rising" / "2. Canyon Rumble" / "3. Encounters in Space"; the Rising→Canyon gate retired after `evalGate` was proven — the shell's lock rendering stays). Rows re-minted in place — dry-run + post-mint hash checks proved only the tutorial rows (+ the gameSpec, for the menu edits) changed, matrix rows byte-identical — the 10 controllable emit-channel char pins re-pinned (the director source is stringified into every controllable page), and each mission's stored row verified by a two-branch headless engine drive (real inputs through the lessons; both wave endings resolve success). Both stored LIGHT (`mapRef`); the 35-row matrix proven **byte-identical** pre/post (dry-run mhash diff vs stored rows). (4) **Game wiring**: `sk_ms_arena` gains the TUTORIAL menu group (first entry, three single-map modes, numbered "1."–"3." and all UNLOCKED — the Rising→Canyon `gate` was the first real consumer of the manifest gate and was retired 2026-08-10 by operator call once `evalGate` proved out end-to-end); [game-shell.js](../control/lib/graph/game/game-shell.js) menu still renders gate-locked mode entries disabled with a "locked — beat <title>" tag (re-evaluated per render, unlocks live — proven capability, currently unexercised; completion auto-records to the `career` progression slice as before). Verified: 18 new director/patrol/dormant engine tests + 9 composer tests (1,171 green across worlds/scene/game/mobile-suit); an end-to-end headless drive of the REAL minted rows through the capture bridge — every Rising step cleared in order by scripted inputs (real vulcan hitscan, real melee adjudication, clip auto-reload edge, combo window) through to the success envelope + career promote; Canyon verified through RECRUIT-at-load, both switches, saber staggers, the z clear, the dormant Geof wake + drop-in + ACE retune.
**Audience:** a new agent/model getting oriented in this repo.
**Supersedes:** the "Unreleased" sections of [control/CHANGELOG.md](../control/CHANGELOG.md) as the current-state snapshot. The changelog remains the release record; this file is what's true *right now*.
**Regeneration contract:** this file is pinned to the working tree above. Committing a staged batch supersedes §6 — rewriting this file (date, branch state, §6) is part of that commit, not an afterthought. (This regeneration catches §6 up to the `v1.0.0` merge; the release's canonical record is [control/CHANGELOG.md](../control/CHANGELOG.md) §[1.0.0].)

Read order for deeper work: [CLAUDE.md](../CLAUDE.md) → this file → the per-subsystem doc it points at.

---

## 1. What mojulo is

**The agent's workshop** — a **single-user, self-hosted substrate** that turns conversations into things that keep existing after the chat ends. Its real interface is a **localhost HTTP MCP server** (~144 tools); the dashboard, chat builder, and wizard are different faces over the same primitives, and the operator drives everything from a host MCP agent (Claude Code / Codex). The substrate's self-description is "PLAYful Cloud" — **P**ersistent, **L**ocal, **A**gent-**Y**oked: bindings keep running while the operator sleeps, everything lives on the operator's host, and **the agent is the only intelligence in the loop** — inference, vision, and authorship stay on the agent, so mojulo holds no LLM credentials on those paths. Five paradigms are one category: **durable bindings minted from a conversation**.

Five creatable artifact paradigms (order is doctrinal — Media before Game, because a game is composition):

| Paradigm | What it is | Entry tool |
|---|---|---|
| **Bot** | Deployed chatbot process (Docker/Fly), own SQLite, hash-chained turns | `infer_intent` → `save_modular_bot` |
| **Connected Service** | Skill synthesized into the host adapter, or a materialized mcp-orbit composition over the operator's installed MCPs | `get_catalyst` / `meta_context_declare_inventory` → `bind_primitives` |
| **App** | Local long-running process + MCP sidecar; inference parked back on the agent via the agent-tasks queue | `install_scaffold` → `meta_context_commit({type:'app_materialization'})` → `start_app` |
| **Media** | Creative artifacts — diagrams, views, walkable worlds, figures, audio, films, publications — as tiny deterministic recipes, never renders (§4) | `create_sketch` / `create_view` / `compose_world` / `create_beats` / `cook`, routed by `get_creative_toolset` |
| **Game** | Composition over Media: levels-as-worlds + music/art bound to a shell + typed store; completability-gated at mint | `create_game` |

Media's full span — scientific views, figures, synthesized audio + song, voice registers, films, publications, *designed pictures* handed to an external image worker — is §4.

Two-package monorepo: `control/` (Next.js 16, port 3001 — bot factory, dashboard, MCP server) and `lite-template/` (Express 5, port 3000 — the bot runtime staged into per-bot artifacts and published as the GHCR image).

## 2. The orientation layer (Ring 0) — how a new agent self-orients

[control/lib/mcp/tools/context.js](../control/lib/mcp/tools/context.js) is the whole progressive-disclosure design in one file:

- The MCP `initialize` preamble is deliberately short (~250 words — it names all five paradigms, Media before Game). Everything heavier lives behind **`forward_context`**, which is an **index, not a glossary**: a lean opener, a *routing index* (user-framing → entry-tool rows), a drawer directory, the commitment-level floor, and two standing safety one-liners.
- **Drawers** (pull on demand): `get_register_kit` (concept glossary), `get_tool_index` (full ring-grouped one-liner index), `get_deliberation_overview` (Ring 6 structural model), `get_ui_map` (dashboard pages), `get_substrate` (PLAYful Cloud positioning).
- **Register tuning**, two axes resolved per call as *override > operator anchor > defaults*: `vocabulary_register` (plain / mixed / mojulo — user-facing phrasing only; concept names and tool descriptions are invariant) and `procedural_disclosure` (terse / reflective / pedagogical narration).
- **Commitment-level floor** — four gate distinctions that must stay legible at every register: *proposed vs materialized*, *dry-run vs promoted*, *watched vs read-once*, *sealed in the audit trail vs not recorded*.
- The `custom_protocol` author's guide also lives here; its Step-0 posture check disambiguates **protocol** (fires inside a bot turn) vs **catalyst** (after-the-conversation workflow) vs **skill** (personal automation) — the most common misfire.
- A registry-sweep test in `context.test.js` enforces that every listed tool appears in `TOOL_INDEX`; adding/removing a main-flow tool means updating `TOOL_INDEX` + `ROUTING_INDEX`. A new `create_view` kind or `compose_world` base needs a **view-vocab card**, not an index row.

## 3. The ring model (registration order in [server.js](../control/lib/mcp/server.js))

Pure JSON-RPC 2.0 dispatch (`dispatchMcpRequest`); the Next.js route owns HTTP + bearer auth; `userId` is always `'local'`. Loopback-only, no auth layer — never expose or tunnel it. `listed:false` keeps retired tool names executing as unlisted aliases. All invocation flows through `instrumentedInvoke` (telemetry, new this branch).

- **Rings 0–1, build & operate.** Intent → protocol recommendation → identity/form/appointment/triage/optical-read config generation → `save_modular_bot` compiles a zip. The composer ([control/lib/composer/](../control/lib/composer/)) stacks protocol cartridges (`00_base` … `05_optical-read`) into `instructions.txt`; [deployers/docker.js](../control/lib/deployers/docker.js) stages lite-template, writes config, bakes `config/embeddings.json`, zips. Five protocols ship: `knowledge`, `formGathering`, `appointments`, `triage`, `opticalRead`. Operate reads proxy through [bot-proxy.js](../control/lib/deployers/bot-proxy.js) — **conversation data never enters the control-plane DB**. Fleet tools aggregate; `verify_fleet_chains` walks the per-turn `content_hash`/`chain_hash` tamper-evident chain across bots, extending across federated triage handoffs (`handoff_hash` = sender's tip-of-chain).
- **Ring 6, deliberation.** The contextmap (`meta_context_brief` / `meta_context_commit` — append-only *why*, six typed commit events), inventory declaration (replace-semantic snapshot of the operator's installed MCPs), vendor capability research (`record_mcp_capabilities`, transactional supersession with history), the mcp-orbit composer (five component kinds: mcp × trigger × pattern × idempotency × render; five per-provider states routing remediation), `bind_primitives` (fills a role template with the operator's *actual* installed tool schemas), `bind_trigger` (cron activation, fires only when the scheduler daemon is enabled), `declare_skills` (host-skill mirror), and `semantic_search` (fuzzy recall over all durable state — principles, capabilities, catalysts, sketch/view vocab, painted-landscape glyphs).
- **Ring 7, apps & runtime.** `install_scaffold` → materialization commit → `start_app` (app + MCP sidecar spawned atomically). The app POSTs inference to `/api/app-inference/envelope`; the agent fulfills via `pull_agent_task` → `submit_envelope_inference` (canonical loop: the `run-inference-worker` catalyst, or `MOJULO_AGENT_RUNTIME=claude-code-headless`). Daemon host gating: `MOJULO_DAEMONS=enabled`, per-daemon `MOJULO_TRIGGER_RUNTIME` / `MOJULO_APP_RUNTIME`. Agent-routed builder chat: `emit_chat_signal` / `request_chat_decision`.
- **Ring 8, plan mode.** Plans are the PROPOSED layer (contextmap is sealed reality). Lifecycle: `forge_plan` → `revise_plan` → `compile_plan` (validates the manifest against the *live* tool registry) → `execute_plan` (per-execution `confirm:true` gate, stops on first failure). `sketch_plan` previews the manifest as a pipeline diagram.
- **Ring 9, stash · gather · cook (+ legacy research).** Typed intake (`mint_stash` → `gather`, 8 item types validated at the gate) → `cook` nucleates ONE outcome per singular aim into a self-contained folder at `/outcomes/<cook_ref>/` (14 publication kinds, essay → comic → `site`; the **agent** authors `report_md` — no server-side LLM). `forge_publications` cooks 2–4 candidate kinds at once; `recommend_kind` is rules-based. Legacy diffuse-research tools coexist behind `enter_research_mode`.
- **Ring 10, visualization** — the largest surface; see §4.
- **Ring 11, operations mode — deprecated (this branch).** The domain-scoped deliberation surface (7 MCP tools `enter_operations_mode` / `forge_ops_tag` / `bind_to_ops_tag` / `unbind_from_ops_tag` / `get_ops_tag` / `list_ops_tags` / `revise_ops_tag`, the `/operations` dashboard + REST routes, and its routing rows) was removed as unused — no subsystem consumed the descriptor/thesis semantics. The `ops_tags` / `ops_tag_members` tables + [OpsTagRepository](../control/lib/db/repositories/ops-tags.js) are **retained as the resource-grouping store for motion**: `forge_motion` / `stitch_motion` forge one tag per "Motion Project" and bind the subject stash + rendered motion as members; the `/motion` gallery groups by them via `listTagsForMember`. Nothing registers at Ring 11 anymore.

## 4. Visualization substrate ([control/lib/graph/](../control/lib/graph/))

**Recipe→render architecture**: stored manifests are tiny recipes; per-kind assemblers regenerate geometry deterministically on render. [worlds/world-scene.js](../control/lib/graph/worlds/world-scene.js) is the single `kind → assemble*Scene` dispatch seam feeding both the live World route and `.glb` export; per-kind descriptors (assembler, title, `walk`/`fogBoxes` capability flags) live in the [worlds/world-kinds.js](../control/lib/graph/worlds/world-kinds.js) registry, one row per kind.

Four renderer backends over one engine-agnostic payload (`{faces, cameras, viewBox, unitScale, …}`):

1. **SVG** — two-point-perspective flatten (`projectTwoPoint`); the "Scene / looked-at" tier. `sketch/` handles manifest derive/diff/validate/PNG.
2. **CSS-3D** — [scene/scene-css3d.js](../control/lib/graph/scene/scene-css3d.js): every planar face → a `matrix3d`-positioned div; dependency-free HTML; live turntables via `create_solid_turntable`. Baked lighting model in [scene-css3d-lighting.md](scene-css3d-lighting.md).
3. **three.js / WebGL** — [scene/scene-three.js](../control/lib/graph/scene/scene-three.js): traversable Worlds with orbit/WASD, vendored three, exportable via `export_model` (.glb). The "World / moved-through" tier. The emitter is registry-driven end to end: per-channel page scripts live as one module each under [scene/channels/](../control/lib/graph/scene/channels/) (registry + contract assertion in `channels/index.js` — `RUNTIME_CHANNELS` rows carry filter/script/header/`__mojStep` slot + `provides`/`requires`; `SETUP_CHANNELS` rows carry the setup/post splice order scene-three consumes via `channelSetupSection`; `scene/channels.js` is the transitional barrel until S5), the channel contract written down in [channels/contract.md](../control/lib/graph/scene/channels/contract.md) + [channels/emit-scope.md](../control/lib/graph/scene/channels/emit-scope.md), the headless-capture bridge names in [scene/capture-contract.js](../control/lib/graph/scene/capture-contract.js), string-safety + delivery-mode helpers in [scene/emit-util.js](../control/lib/graph/scene/emit-util.js). Emission is pinned byte-level by a 46-fixture characterization net (`emit-channels.char.test.js`) + a V8 parse gate (`emit-parse.test.js`) + a cross-block symbol lint (`channels.contract.test.js`); AO bakes are LRU-memoized in-process (geometry-keyed, colors excluded — ~24× on repeat renders).
4. **GLSL raymarch** — painted-landscape raymarch + the volumetric effects overlay (fog) that composites OVER three.js worlds, occluding via a grid-culled scene SDF. Principles in [raymarch-effects-layer.md](raymarch-effects-layer.md).

Shared lighting primitive: `polygonizer/vexar.js` (directional Lambert, one dot product per face). Material response now rides that same baked solve as an opt-in per-surface `material` channel (§6 item 1). Subsystem weight: `polygonizer/` (~321 files — the geometry core), `views/` (~81 — math/science/bio + view-vocab cards), `worlds/` (~32 — physics sim, event bus, game idioms, controllable worlds, `operator-world.js`), `game/` (store kernel, mechanics, kits, and the new glyph/sfx UI-language shelf), `architecture/` (fractal-condo, `fractal-school.js`, dungeon-designer, and `edifice.js` — the graph-authored bespoke building), plus `scene/`, `effects/`, `landscape/`, `machina/`, `figures/`, `vehicles/`, `vehicle-designer/` (the veh-* part shelf: 9 parts + garage attach contract + BOM audit behind one registry barrel), `city/`.

Entry tools by intent: `create_sketch` (diagrams/charts; vocab via `semantic_search({kinds:['sketch_vocab']})` + `get_sketch_vocab`; now also the mint for the `image-outcome` / `sequential-art` designed-picture kinds — see §6) · `create_view` (45 science/math/bio study kinds; manuals via `get_view_vocab`) · `compose_world` (base × theme: city, transport-hub, controllable, action games, operator-state, planetary, painted-landscape, math, school) · `create_figure` (posed protoform bodies, gait GIFs; `emote_figure` plays named body-language emotes on a stored figure — §6) · `create_beats` (synthesized audio artifacts — ambient loop / composition / pattern groove / SFX; a composition **sings** via a `patch:'voice'` part — the in-process parametric vocaloid, no new tool or route; cards via `get_beats_vocab` + `semantic_search({kinds:['beats_vocab']})`) · `create_voice` (deterministic voice-register recipes for narration/voiceover; WAVs via the voice worker seam — §6) · `create_game` (playable level artifacts over the game store/contract spine; completability-gated at mint; `get_game_vocab` serves five families — slice / mechanic / kit / glyph / sfx) · `create_workbench` / `create_assembler` (lathe polygomers → assemblies; `skin_polygomer` binds a painted skin — §6) · `create_carved_solid` (extruded metal wordmarks) · `create_manji_tree` (cardinal-grammar IR; `sketch_polygomer` is the parts-grammar door for free-armature creatures/objects) · `create_edifice` (bespoke walkable buildings from mass/concourse graphs; advisory livability, never gated) · `forge_motion` / `stitch_motion` (camera clips, chart decks, MP4 films) · `request_image_render` → `pull` / `submit` / `accept_image_render` (the durable render-worker handoff — §6) · `reference_protocol` / `capture_reference` (the agent as vision adapter for photo references — no image ever sent to mojulo) · `measure_view` (real-unit time-series read-back) · `verify_machina` (checked feasibility verdicts). Ring 10's per-form tool manuals now live behind `get_creative_toolset({form})` — the FORM-scoped subdrawer reader (§6).

## 5. Bot runtime ([lite-template/](../lite-template/))

Express 5 + better-sqlite3. Fully **local RAG**: `helper/vector-rag.js` boots from the baked `config/embeddings.json` and embeds queries in-process via `helper/embedder-local.js` (`Xenova/multilingual-e5-small` ONNX q8) — same vector space as the control-plane build; fails loudly, no keyword fallback. Turns are hash-chained (`content_hash` + `chain_hash`, SHA-256); `/handoff` writes `event_type='handoff'` rows carrying the sender's tip-of-chain for federated triage. Prometheus `/metrics`. Embeddable widget via `<script src="${botUrl}/widget">`. Landmines: onnxruntime-node is glibc-only (Debian slim, never Alpine); model weights are gitignored and fetched at install.

## 6. The 1.0.0 release — what landed on `main` (this is what supersedes the changelog)

**`v1.0.0` is merged to `main` and tagged.** The visualization-era batch and the 2026-08-06 late-run batch below both landed together via the release merge. The visualization-era batch was cut with one editorial boundary: **the mobile-suit extraction**. The repo keeps everything general — the controllable-world combat/match engine (incl. gamepad support, practice mode, the controller pause UI), the walking-unit layer (unit-pose/anchors/rig, dodge-poses, ms-materials, pose-lab), the four controllable emitter blocks, and the `mobile-suit-builder` catalyst — while the game's *content* (armory + weapon shelves, shields, arena maps + atmosphere, liveries, suit vocab cards, 43 construction scripts) lives in the gitignored pack [lib/graph/mobile-suit/](../control/lib/graph/mobile-suit/) behind guarded lazy-import seams that degrade clean when the pack is absent — verified in BOTH states (uninstalled: 2,346 tests green, char pins byte-identical; installed: full suites green). Game content is an artifact, not source; the extraction's master doc is `lite-template/integration/archive-mobile-suit/extraction.plan.md`. Also in the batch: the game-project layer + Mojulo Arcade + Game Developer studio, the pixelizer 2D register (brickster + philosopher's stone), the sprite-sheet / cover / `export_game` seams, the mojulo-orient consent-first MCP, the `lib/graph/` folderization second pass, the plan-file consolidation sweep, and the 1.0 release hardening (pixelizer test fixtures restored from the archive, `create_cover` indexed into the illustration form drawer + `RING10_TOOLS`, description-budget re-pins with the payload ceiling at 372k (measured 371,050), vitest `hookTimeout` 30s beside the existing `testTimeout` headroom). The release also **promoted Media to a first-class paradigm** — five paradigms, Media before Game because a game is composition — swept through every orientation surface by the PARADIGMS drift test (preamble, opener, get_substrate, all three register glossaries, worked examples: `creative-mint` renamed `media` with the old key aliased), with a twelfth conscious char re-pin absorbing the operator's final 08-04 emitter edits (space melee, controller diagram, map carousel). Full gate at the boundary: **414 test files / 6,039 tests green**. [control/CHANGELOG.md](../control/CHANGELOG.md) §[1.0.0] is the canonical record; per-thread detail lives in the archived plan files and the `control/docs/` orientation docs.

### Late-run batch — folded into the release (2026-08-06)

On top of the visualization-era base sits a coherent batch that grew after the 08-04 boundary and was folded into `v1.0.0` before the merge. Four threads plus spike tooling; its per-thread build logs live in the plan files named below.

1. **Controllable-world decomposition — LANDED, complete** ([worlds/controllable-split.plan.md](../control/lib/graph/worlds/controllable-split.plan.md), S0–S4). The 3,305-line `worlds/controllable-world.js` monolith — the whole action-game engine as one import-free closure — is split into a set of **import-free builder closures composed over a shared namespace `E`** (`composeControllable`), so the single-source-of-truth browser emission survives per-system. New [worlds/controllable/](../control/lib/graph/worlds/controllable/): `compose.js` (kernel + the ordered `EMISSION`/`EMISSION_ENGINE` lists), `core.js`, `gait.js`, `rules-basic.js`, `rules-platform.js` (with the S4 maneuver-phase registry), `combat-{hit,ranged,melee,match}.js`; MS content lifted into the gitignored pack's `ms-maneuvers.js`/`ms-ai.js`. `stepWorld` is now a **registered slot-runner** (order pinned by an exact `toEqual`, not a snapshot). The façade `controllable-world.js` shrank 3,305 → 86 lines, re-exporting the same API; `all.js` was dissolved. Gate: **four golden scenario traces** (`controllable-world.trace.test.js`, new + its snapshot) held byte-identical through every carve; the parity test generalized to a per-builder `new Function` self-containment tripwire; char-net re-pinned (the same 10 controllable-channel fixtures — every carve changes emission bytes by design; non-controllable hashes unchanged).
2. **Drivable + flyable vehicles — LANDED** ([vehicle-designer/drivable-vehicles.plan.md](../control/lib/graph/vehicle-designer/drivable-vehicles.plan.md), D1–D5 + gamepad + fly; D4's visual frame-strip eyes-gate is the one open remainder). The **first external tenants of the split architecture** — a rule-level builder needed zero engine edits, as the split promised. The `drive` rule (`worlds/controllable/rules-drive.js`) is a pure kinematic longitudinal-dynamics controller (real `F=P/v` power curve capped by traction, quadratic drag, bicycle-model steering off the rig's wheelbase, distance-true wheel spin); `deriveVehicleRig`/`bakeVehicleRig` ([vehicle-designer/vehicle-rig.js](../control/lib/graph/vehicle-designer/vehicle-rig.js)) classify wheels FL/FR/RL/RR by position and pack the browser's rig-figure format with a single `roll` clip; the drivetrain is seeded ON the parts (`VEH_ENGINE_DRIVETRAIN` in [veh-engine.js](../control/lib/graph/vehicle-designer/veh-engine.js), `curbMass`/`brake` on `VEH_ARCHETYPES` in [assembly-bom.js](../control/lib/graph/vehicle-designer/assembly-bom.js)) and composed by [drivetrain.js](../control/lib/graph/vehicle-designer/drivetrain.js) `deriveDrivetrain` (no engine part → null → not drivable, by data). World binding is `figures.vehicleRef` in [worlds/world-scene.js](../control/lib/graph/worlds/world-scene.js) beside `unitRef`, accepted in `KNOWN_RULES`/mint validation ([scene-controllable.js](../control/lib/mcp/tools/scene-controllable.js)). Board/drive/dismount rides the existing pilot-swap (T). Alongside: a second feel-model builder `rules-fly.js` (energy-and-attitude flight, stall, coordinated bank-to-turn), **gamepad RT/LT throttle-brake mapping** keyed off the live pilot each frame, opt-in mesh `tilt` sync (banking planes). New tests: `worlds/rules-{drive,fly}.test.js`, `worlds/drive-world.smoke.test.js` (parses every emitted script tag — a tripwire after a template-literal backtick escaped `node --check`). Vocab: [sketch-vocab/veh-drive.md](../control/lib/graph/sketch-vocab/veh-drive.md) (the affordance-fact table later fx/sfx bind against). This closes plan item 9's drivable leg (below).
3. **Orientation containment — C1–C3 LANDED** ([mcp/tools/orientation-containment.plan.md](../control/lib/mcp/tools/orientation-containment.plan.md); C4 schema diet open). `forward_context` gains a stateless `mode: 'office' | 'studio'` (default office): the eight FORM recognizer rows + the GAME row relocate **verbatim** from the always-paid office body into a new standalone studio body (reached by one hook row), venting the body that had been jammed at ceiling. The single `BODY_CEILING` (11,700) retires for per-mode pins — **office 9,600 (measured 9,435, was 11,623), studio 7,400 (measured 7,169)**. `SERVER_INSTRUCTIONS` Media/Game bullets collapse to one line each teaching the two wings. Touches [context.js](../control/lib/mcp/tools/context.js), [server.js](../control/lib/mcp/server.js), `context.test.js`, `routing-cards.test.js`, `routing-eval.integration.test.js`. The description-budget pins were **re-pinned upward** in the same batch (`tool-descriptions.test.js`, uncommitted): `PAYLOAD_CEILING` 372,000 → **376,000** and three catalyst descriptions (`mint_catalyst` / `list_catalysts` / `custom_catalyst`) added to the allowlist — done to bless the **committed** local-catalyst shelf ([catalysts/local-catalysts.plan.md](../control/lib/mcp/catalysts/local-catalysts.plan.md)), and the same headroom absorbed the mode schema's +206 bytes. Suite green, but the payload sits at **374,898 / 376,000 — only ~1.1K headroom**, and `create_manji_tree` alone is 54.7K (15% of the payload). C4 (the schema diet that ratchets the payload back DOWN by moving `create_manji_tree` / `forge_motion` enums behind their vocab cards) remains **open** — the blessing bought room, it did not shrink the surface. The successor [tool-packs.plan.md](../control/lib/mcp/tools/tool-packs.plan.md) (PROPOSED) re-cuts the whole surface into ~19 result-shaped packs; its P0 host spike **executed and found `tools/list_changed` is a dead end on Claude Code today** (host never re-lists mid-session; anthropics/claude-code#66084) — so packs park behind a dispatcher-hybrid fallback, and the near-term economic fix reverts to C4's schema diet.
4. **Fog `depthClip` — LANDED, opt-in** (engine seams general-repo; its `map-treatment.plan.md`/`ms-contrast.plan.md` drivers live in the gitignored `mobile-suit/` pack and will not commit). Volumetric fog gains an opt-in `depthClip`: a per-frame depth prepass clamps the raymarch at the rasterized scene's foreground meshes (rigged suits, walkers, cars) that the box-field SDF doesn't carry, so fog stops painting over them. Absent → the composed frag and every emitted page stay **byte-identical** (char-pin safe). Touches [effects/effects-fog.js](../control/lib/graph/effects/effects-fog.js), [effects/volume-raymarch.js](../control/lib/graph/effects/volume-raymarch.js), [scene/scene-three.js](../control/lib/graph/scene/scene-three.js).

Plus spike tooling (untracked, not part of the substrate): eyes-gate mint scripts `control/scripts/mint-{drive-world,map-lab,contrast-lab}.mjs`. Recorded gate at the release: **6,075 tests green**; the description/payload ratchets are green on the re-pinned ceilings above (the C4 downward diet is a deferred optimization, not a failing gate). This batch was folded into the `## [1.0.0]` CHANGELOG section (see §[1.0.0] "Late additions") and merged to `main` as part of `v1.0.0`.

What's open is staked out in plan files on the tree, roughly in order of pull:

1. **Platformer** ([platformer.plan.md](../control/lib/graph/game/platformer.plan.md)) — PROPOSED, analysis done, no code: the small copyable reference 3D collectathon (the arena is a showcase, not a pattern) + the `platformer` kit promoting the three missing substrate verbs (moving-platform carry, platform-island authoring, platformer camera), so "make me a 3D platformer" becomes fill-in-the-blanks. Operator decisions already taken: minimal teaching reference; land the reusable substrate; dream the hero + level concepts via the Qwen loop; lean on the existing game-UI language. The natural top pull — it closes the loop the game-project/Arcade/pixelizer batch opened.
2. **Game-publish phase 3+** ([game-publish.plan.md](../control/lib/graph/game/game-publish.plan.md)) — the agent-driven GitHub push + publication provenance recorded back, over the landed `export_game` folder.
3. **Sprite-sheet P2/P3** ([sprite-sheet.plan.md](../control/lib/graph/image-outcomes/sprite-sheet.plan.md)) — the first pixelizer game consuming baked sprites; the machine gate.
4. **Game-developer GP4/GP5** ([game-developer.plan.md](../control/lib/graph/game-developer.plan.md)) — membership-chip surface hygiene; demand-gated extras. Plus the pixelizer boundary sweep ([polygonizer-pixelizer-cleanup.plan.md](../control/lib/graph/polygonizer-pixelizer-cleanup.plan.md)).
5. **Character-from-dream C4** ([character-from-dream.plan.md](../control/lib/graph/polygonizer/character-from-dream.plan.md)) — keyframe guides sourcing the body recipe (`renderFigureWithArmature` landed as its substrate piece); plus the deferred figure-3D-skin leg (`figure` as a `WORLD_KIND`).
6. **Voice V1–V3** ([voice-worker.plan.md](../control/lib/graph/voice/voice-worker.plan.md)) — the durable voice-render bicycle, jp-female pole calibration (`calibrated: false`), consumers (bots/worlds/publications), en/zh banks. The voice-sfx spike awaits its ears-gate ([voice-sfx.plan.md](../control/lib/graph/voice/voice-sfx.plan.md)).
7. **Song S1/S3/S4** ([beats-song.plan.md](../control/lib/graph/beats/beats-song.plan.md)) — `authorLyrics` + melody import, consumers, the realism ceiling (melisma, humanize, SATB).
8. **Shape-from-dream D1–D3 + DF** ([shape-from-dream.plan.md](../control/lib/graph/polygonizer/shape-from-dream.plan.md)) and **animal-from-dream A1–A4** ([animal-from-dream.plan.md](../control/lib/graph/polygonizer/animal-from-dream.plan.md) — `create_animal` is the gating prerequisite).
9. **Vehicle-designer VB→VF** ([vehicle-designer.plan.md](../lite-template/integration/plan-archive/vehicle-designer.plan.md)) — display assembly, `vehicle-livery`, catalyst + routing card, meta-fabricator graduation. The landed V0–VA shelf (9 part modules + `veh-garage` attach contract + `assembly-bom` audit) is consolidated in [vehicle-designer/](../control/lib/graph/vehicle-designer/) with a registry barrel (`index.js`: `VEH_PARTS` keyed by the same kind strings as the archetype BOM rows, `resolveVehPart`, `listVehShelf`). VE (the wheel-spin rig) was promoted and expanded into **drivable vehicles** ([drivable-vehicles.plan.md](../control/lib/graph/vehicle-designer/drivable-vehicles.plan.md), D1–D5 + gamepad + fly) — **landed in the late-run batch above** (drive/fly rules, vehicle rig, drivetrain seeding, `vehicleRef` world binding); the only remainder is D4's visual frame-strip eyes-gate. VB→VF proper (display assembly, `vehicle-livery`, catalyst + routing card, meta-fabricator graduation) remains open.
10. **Edifice E6+** ([dream-architecture.plan.md](../control/lib/graph/architecture/dream-architecture.plan.md)) — chambers/dungeon interior kernels, per-floor slabs + stairs, egress/flow advisory, round masses, the monument track.
11. **Agent-toolchain C1/C3/C5** ([agent-toolchain-capability.plan.md](../control/lib/mcp/agent-toolchain-capability.plan.md)) — queryable capability gate, conformance probe, a standing local image worker. A future local Qwen host adapter is staked in [qwen-host.plan.md](../control/lib/mcp/adapters/qwen-host.plan.md). Pre-existing: figure-animal → sketch graduation ([figure-animal-graduation.plan.md](../control/lib/graph/polygonizer/figure-animal-graduation.plan.md)).

(Two former list items left the tree in the consolidation sweep: **level-from-dream** — closed, its style→shell ambitions carried forward by the ground-textures vocabulary and the landed dungeon style bible — and **mobile-suit-builder M3/M4** — the build log archived into the `control/docs/` orientation docs; the [`mobile-worker`](../control/lib/graph/sketch-vocab/mobile-worker.md) and [`g-series`](../control/lib/graph/mobile-suit/vocab/g-series.md) vocab cards remain the register's canon.)

**Implication for a new agent:** the changelog's tool inventory is stale for the view family — do not look for `atom_view` / `black_hole_view` / etc. as individual tools; they are kinds under `create_view`. Keep `TOOL_INDEX` / `ROUTING_INDEX` in context.js in sync when touching tools (the registry-sweep test catches drift); Ring 10 tool manuals live in FORM subdrawers behind `get_creative_toolset`, and a new creative capability means a routing card carrying its `form` + a toolset entry, not a fatter flat drawer. Renders, skins, character sheets, and WAVs are **bound derived artifacts** — append-only files under `data/outcomes/<ref>/` with provenance, never rows in the DB; the recipe in `sketches` stays the sovereign artifact. Same posture for the World emitter: a new channel is a module under `scene/channels/` + a registry row in `channels/index.js` (`RUNTIME_CHANNELS` or `SETUP_CHANNELS`, with its `provides`/`requires` declared) plus an emit-fixtures entry, NOT hand-wired splices in scene-three.js — the load-time contract assertion and the `channels.contract.test.js` symbol lint fail on undeclared cross-block coupling, and any change to emitted bytes fails the char-net snapshots, which may only be re-pinned when the change is intended (kernel edits count: the beats/physics/controllable kernels are stringified into pages).

## 7. Invariants a new model must not violate

- Conversation data never moves into the control-plane DB; per-bot reads go through the bot proxy.
- All builder paradigms converge on `buildDeploymentConfig()` — no paradigm-specific branches downstream.
- Bot turns go through the hashing helpers; never bypass `content_hash` / `chain_hash`.
- The bot image is bot-agnostic; Fly injects per-bot config as files. Pin exact bot tags, never `:latest`.
- `forward_context` stays a thin routing index; heavy orientation lives behind drawers.
- MCP transport binds to localhost only; no tunnels, no auth layer assumed.
- The dashboard is not a conversational surface (the bot builder chat is the deliberate exception).
- Never `cat`/`Read` a `.env` under `$MOJULO_HOME` or in an unzipped bot — use `inspect_bot_env`.
- No intent-classification / use-case gating layers; capability and suitability assessments belong to the operator ([responsibility-model.md](responsibility-model.md)).
- Synthesize ≠ certify: dry-run on one real input → inspect → promote. Re-validate on schema drift.

## 8. Design signature (the taste that should carry into new work)

- **Progressive disclosure everywhere**: thin index → drawers → vocab cards; context cost is paid on demand, never front-loaded.
- **Recipes, not renders, in the DB**: tiny deterministic manifests regenerated on render (a painted landscape is ~30 tokens vs ~14K of raw SVG).
- **Append-only deliberation separated from replaceable present-state**: contextmap (why, sealed) vs inventory (what's installed, replace-semantic) vs outcomes (what fired, never written to contextmap).
- **The agent is the inference and vision layer**: apps park inference on the queue; photo references are read by the agent's own eyes; cooks are agent-authored. Mojulo needs no keys on those paths.
- **Closed vocabularies + seeded determinism** for generative surfaces: glyph cards, heartbeats/splatches, view-vocab — content-extensible by dropping a card, code-extensible only for new families.

## 9. How mojulo accreted (origin & stance)

None of the architecture above came from a roadmap. It accreted, each layer built exactly deep enough to unblock the next — and that history explains the shape better than any spec would.

**The chain.** Mojulo began as an enterprise/sovereignty play, disrupted by frontier-lab capability announcements; the author built in stealth and pivoted continuously before open-sourcing the initial chatbot layer. An MCP experiment then unlocked the connected-services platform → **metacontext** became necessary so composed tools wouldn't sprawl or repeat → which extended naturally to vibe coding, then spec writing → mojulo was already Node.js, so a **runtime** followed → then the idea of the agent writing to the app and harnessing the filesystem + event bus → the system got complicated enough to need a **chart-rendering tool to depict itself**. Then, for reasons unknown, the author applied two-point perspective, draughtsmanship, and mandala principles to fake perspective with divs and CSS → **pure-world**: all vector, no pixels, map everything there first → frame-by-frame animation (each shot is a moment in time; frames stitch to GIFs, GIFs stitch to movies) → the **figure** → three.js made worlds walkable → live objects moving in code brought in **physics** via experiments → which accreted into the science explainers, culminating in the black-hole viewer → and Newtonian physics was clawed back into the world renderer and its own event bus.

**Stances a new model should hold:**

- **No onboarding is the product.** The coined lexicon isn't a tax to be mitigated — the ask-and-discover loop is the intended interface. You ask what it can do, it gives you clues, and agent + operator build something interesting together. The bug class to watch for is *dead-end clues* (terms that don't reward the question), not unfamiliar terms per se.
- **The frontier moves; layers get second passes on demand.** Surfaces held at Phase-1 depth are not debt — each was built to unblock its successor. A layer deepens when something downstream demands it (physics → world renderer is the exemplar). Per task, ask: is this layer still at the frontier, or has something started demanding its second pass?
- **Pure-world (all-vector, no-pixels) is the load-bearing constraint** of the entire visual arm. It's what keeps recipes tiny, makes animation stitching rather than rendering, makes diffs structural, and makes real-unit read-back (`measure_view`, SI-honest experiments) possible. New visual work should keep the world pure-vector.
- **Happy to fail.** The visualization shift adds positioning complexity *and* a unique shot at enabling users to one-shot something that can be 'negotiable' and 'workable', giving each user the opportunity to eventually create something nobody has done. Weight advice toward "does this make the one-shot better and the failure legible/cheap," not conventional prudence. 
- **The self-depiction loop is a compass.** Twice now, tools built to depict the system's own complexity have become product (chart rendering → Ring 10). When guessing what accretes next, trust that engine.
