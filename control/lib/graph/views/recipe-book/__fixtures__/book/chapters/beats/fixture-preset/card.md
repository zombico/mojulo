---
{
  "id": "fixture-preset",
  "name": "Fixture preset (beats twin)",
  "entry": "create_beats",
  "summary": "Shares its id with the view fixture card — dedupe is per catalog, so BOTH survive.",
  "when": "per-catalog dedupe test fixture"
}
---

The beats twin of the view card `fixture-preset`.
